# 개선 필요사항 종합 (전체 리뷰 결과 — 2026-07-02)

> 이 문서는 **코드 리뷰 + 오픈소스 벤치마크**(Immich·PhotoPrism·LibrePhotos·Photoview·Damselfly·Czkawka·dupeGuru·synology-api)
> **+ UI/UX 벤치마크**(Google Photos·Immich·Synology Photos·Finder·NN/g)의 결과를 통합한 **작업 기준 문서**입니다.
>
> **규칙: 모든 구현 작업은 착수 전에 이 문서의 관련 절을 확인하고, 해당 항목을 반드시 반영한 뒤 체크박스를 갱신합니다.**
> (프로젝트 `CLAUDE.md`에 강제 규칙으로 명시됨)

---

## A. 백엔드 수정 — 1단계 기능 개발 전 선행

### A-1. [HIGH] 로그인 자격증명이 GET 쿼리스트링으로 전송됨
- [x] 수정 (2026-07-02) — `_send`에 POST 지원 추가, `login()`을 POST 폼으로 전환
- 위치: `backend/app/dsm/client.py` — `_send()`가 항상 `http.get(url, params=...)` → 로그인 시 비밀번호가 URL에 실려 DSM 웹서버·리버스 프록시 로그에 남을 수 있음.
- 수정: `_send`에 POST(form body) 지원 추가, 최소한 `login()`은 POST로 전환. DSM 7 `SYNO.API.Auth`는 POST 지원.

### A-2. [HIGH] DSM sid 만료 처리 부재 (앱 세션과 수명 불일치)
- [x] 수정 (2026-07-02) — `main.py`에 `DsmError` 예외 핸들러 추가. 세션 무효 코드 → 401 + 세션/쿠키 삭제, 그 외 → 502. `system.py`는 핸들러에 위임.
- 앱 세션 TTL 8h(`config.py`) vs DSM sid는 DSM 보안 설정에 따라 **15분 만에 만료 가능**(기본 최대 7일 — [DSM Login Web API Guide](https://kb.synology.com/en-global/DG/DSM_Login_Web_API_Guide/2)).
- 현재 `api/system.py`는 DSM 오류를 502로 반환 → 프론트가 재로그인 유도 불가.
- 수정: `DsmError` 세션 무효 코드 **106(timeout)/107(중복 로그인)/119(sid 무효)**를 공용 예외 핸들러로 잡아 **앱 세션 삭제 + 401 반환**. (당초 105도 포함했으나 105는 "권한 부족"이라 재로그인으로 풀리지 않아 제외 — `errors.SESSION_INVALID_CODES` 참조.) 이후 모든 엔드포인트가 자동 혜택.

### A-3. [MED] 역할 판별을 `is_manager` 기반으로 교체
- [x] 수정 (2026-07-02) — `detect_role`을 `SYNO.FileStation.Info`의 `is_manager` 기반으로 교체, `/homes` 접근은 `detect_can_browse_homes`로 분리. 세션·`UserInfo`·프론트 타입에 `can_browse_homes` 추가(스키마 마이그레이션 포함).
- 현재 `api/auth.py detect_role()` — `/homes` 목록 성공 여부로 admin 판별. **user home 서비스가 꺼져 있으면 진짜 관리자도 member로 오판**.
- 수정: `SYNO.FileStation.Info`(get)의 **`is_manager`** 필드로 role 판별. `/homes` 접근 가능 여부는 별도 capability(`can_browse_homes`)로 분리 — 관리자 UI 활성화 조건과 역할을 분리(명세 4.5의 "사유 안내"와 연결).

### A-4. [MED] 로그인 시도 제한 + DSM Auto Block 대응
- [x] 수정 (2026-07-02, 2026-09-02 보강) — `rate_limit.py`의 SQLite 영속 제한: 계정당 10분/5회에 더해 **실접속 IP당 10분/20회**를 적용해 계정명 순환 우회도 DSM 도달 전에 차단. NPM의 `X-Real-IP`는 운영에서만 명시적으로 신뢰하고, 성공 로그인은 IP 기록 전체가 아니라 선기록 1건만 제거한다. 로그인·로그아웃 SQLite 작업은 `asyncio.to_thread`로 이벤트 루프에서 분리. README/DEPLOYMENT에 DSM 허용 IP와 프록시 헤더 안내.
- 앱이 로그인 프록시라서 가족 누군가 비밀번호를 반복해 틀리면 **DSM Auto Block이 도커 컨테이너/게이트웨이 IP를 차단 → 전 가족 로그인 불가** ([DSM Auto Block](https://kb.synology.com/en-global/DSM/help/DSM/AdminCenter/connection_security_account?version=6)).
- 수정: ① 앱 레벨 로그인 시도 제한(예: 계정당 5회/10분, SQLite로 충분) ② README에 "DSM 허용 IP 목록에 도커 게이트웨이 IP 추가" 안내 ③ sid 재사용 철저(매 요청 로그인 금지 — 현 구조는 이미 준수).

### A-5. [LOW] 인프라/품질 묶음
- [x] `config.py` `session_secret` — 미사용이라 제거(쿠키가 무작위 토큰이라 서명 불필요). `.env.example`도 정리.
- [x] `docker/Dockerfile` — `npm install` → **`npm ci`**, **non-root USER**(appuser) 추가. ⚠️ 이후 예외: 공용(/photo) ACL의 EXIF 판독을 위해 **prod 컨테이너는 root(user 0:0)로 실행**(2026-07-09 사용자 승인, docker-compose.prod.yml).
- [x] `docker-compose.yml` — `/api/health` 기반 **healthcheck** 추가(python urllib).
- [x] `db.py` — `PRAGMA journal_mode=WAL` 설정. (2단계 해시 저장 시 sync SQLite → 스레드풀 오프로딩 재검토)
- [x] `session_store.purge_expired` — 시작 시 1회 + **로그인마다 정리**(별도 백그라운드 스위퍼 없이 주기화).
- [x] `errors.py` — `SYNO.Foto.*`/`SYNO.FotoTeam.*` 에러코드 테이블 추가(실 NAS로 검증 필요).
- [x] 단위 테스트 추가 — `errors.message_for`, `session_store`, `rate_limit`, `DsmClient`(httpx `MockTransport`). **26개 통과**. `requirements-dev.txt`/`pytest.ini` 추가.

---

## B. 프론트 UI/UX 설계 결정 — 화면 구현 시 적용

> **구현 현황(2026-07-02, 1단계 타임라인 마일스톤)**: B-1·B-3 완료, B-2·B-4·B-5·B-6 부분 완료(파일 작업 API/작업기록과 함께 완성되는 항목은 다음 단계). 미체크 항목의 사유는 각 절 참조. NAS 없이 개발 가능하도록 백엔드 **MOCK_MODE** 추가(결정적 가짜 데이터 + SVG 썸네일). DSM 실연동 코드는 `backend/app/photos/dsm_source.py`에 있으며 **실 NAS 미검증**(파일 상단 검증 목록 참조).

### B-1. 타임라인
- [x] 월/일 버킷 **count-first** 방식: 개수 메타데이터만 먼저 받아 섹션 높이 사전 할당 → 스크롤바가 전체 아카이브를 대표 ([Building the Google Photos Web UI](https://medium.com/google-design/building-the-google-photos-web-ui-45b714dfbed1), [Immich Timeline](https://deepwiki.com/immich-app/immich/3.5-timeline-and-asset-display)) — `GET /api/photos/buckets` + `lib/rowModel.ts` 플레이스홀더 행
- [x] 우측 **날짜 스크러버**(드래그 시 연/월 라벨)로 임의 시점 점프 — `Scrubber.tsx`
- [x] **스크러버 ↔ 플로팅 버튼 겹침 해소** (2026-08-02, 사용자 제보: 모바일에서 연도 라벨·홈·화살표 버튼 겹침) — Scrubber가 실제 표시될 때 store `scrubberCount`에 보고하고, NavControls(↑/‹/🏠 스택)가 이를 구독해 레일 폭만큼 왼쪽으로 이동(`right-3`→`right-12`, transition). 스크러버 z를 30으로 올려 드래그 월 버블이 버튼 위에 그려지게 함(버튼은 레일 밖이라 포인터 충돌 없음). MOCK+Playwright 모바일 뷰포트(390px) 연/월 줌에서 검증
- [x] **스크러버 하위 기간 가시성 개선** (2026-09-02, 사용자 제보: 연도만 보이고 그 이하 기간은 없음) — 원인 ① 연 줌이 연도 헤더만 마커로 변환해 월 정보 자체가 사라짐 ② 연도 우선 26px 충돌 제거가 장기 보관함에서 모든 월 텍스트를 밀어냄(19년/700px 모의 계산: 월 0~1개). 연 줌도 실제 보유 월을 연 영역 안에 배치하고, 텍스트 충돌 여부와 무관하게 **모든 월 눈금**(분기·연 경계 강조)을 항상 렌더한다. 텍스트 간격은 연 22px/월 12px로 분리하고 현재 스크롤 위치의 월을 작은 배지로 상시 표시한다. 손잡이는 `scrollTop/(totalHeight-viewportHeight)`와 실제 이동 거리(`railH-thumbH`)로 보정해 맨 아래 위치와 드래그 좌표를 일치시켰다.
- [x] **justified layout**(행 높이 균등, 원본 비율 유지·크롭 없음) — square grid 비권고(가족 사진은 세로/가로 혼재)
- [x] ⚠️ 큰 버킷의 geometry **동기 일괄 계산 금지** — **일 단위 버킷 + 버킷별 지연 계산·메모이제이션**으로 구조적으로 회피 ([Immich #28861](https://github.com/immich-app/immich/issues/28861) 프리즈 사례)
- [x] 공용 공간도 **모든 구성원에게 타임라인 제공** — 공용/개인 동일한 타임라인 UI(권한은 DSM이 enforce)

### B-2. 썸네일 로딩
- [x] **3단계 로딩: thumbhash(즉시 블러) → 소형 → 대형** (2026-07-03) — ThumbHash 인코더 자체 구현(`hashing.thumbhash_bytes`, evanw 레퍼런스 포팅, Pillow-only·numpy 없음; 입력 32px 축소로 ~10ms/장). dedup 스캔이 (sha, phash, **thumbhash**)를 photo_cache에 저장(기존 행은 thumbhash 없으면 재해시), items/folder/person/place 목록 응답에 배치 주입(`fill_thumbhashes`). 프론트는 공식 `thumbhash` npm(3KB)으로 base64→blur dataURL 디코드(메모이즈), PhotoCell 배경에 표시(스캔 안 된 항목은 기존 단색 fallback). e2e: 인코더↔공식 디코더 호환 확인(유효 PNG·종횡비·자연색). **주의: 블러가 보이려면 배포 후 중복 스캔 1회 재실행 필요**(기존 photo_cache 행 채움)
- [x] 1단계에서는 Synology Photos 썸네일(`SYNO.Foto.Thumbnail`) 재활용이 우선 — `dsm_source.thumbnail()` 프록시로 구현(실 NAS 미검증)
- [x] **xl 실전송 절감: WebP 협상 + fetchpriority** (2026-08-02, 사용자 요청 "전송 자체가 빨라지는 방법") — ① 백엔드: `Accept: image/webp`면 xl을 WebP q80으로 1회 재인코딩(`transcode.py`, 스레드 실행) 후 ThumbCache 변형 키에 저장 — 바이트 30-45% 절감, 이득 없는 이미지는 원본을 변형 키에 넣어 재시도 CPU 차단(서빙 타입은 시그니처 판별). ETag에 포맷 포함 + `Vary: Accept`. sm/m은 당시 제외(작은 파일이라 인코딩 CPU가 콜드 그리드를 늦춘다고 판단). "저장" 원본 다운로드는 무영향. ② 프론트: 라이트박스 xl `<img>`에 `fetchpriority=high`(소문자 — React 18) — 그리드 sm 수십 장과의 요청 큐 경쟁에서 우선. pytest 7건(협상·변형 캐시·ETag 분리·304·당시 sm 비변환)
- [x] **sm WebP 후속 적용 — 운영 캐시 실측 기반** (2026-09-02) — ThumbCache 59,173건에서 추출한 80장 비교 결과 q80/method2가 JPEG 대비 총 43.8% 절감, 인코딩 p50 5.65ms·p95 8.88ms로 method4(p50 11.74ms)보다 그리드 콜드 비용이 낮았다. 따라서 `Accept: image/webp`인 sm도 별도 변형 키에 1회 저장하고 `Vary: Accept`·포맷별 ETag를 적용했다(sm=method2, xl=method4, 오류 폴백 m=JPEG 유지). 브라우저/SW의 기존 immutable JPEG를 즉시 교체하도록 썸네일 URL 버전을 `tv=7`로 갱신했다.
- [x] **라이트박스 xl 로딩 중 흰 화면 해소** (2026-08-02, 사용자 제보: 모바일 확대 시 흰 화면 장시간 노출) — 원인: xl `<img>`를 바로 그려 부분 디코딩 영역이 흰색으로 노출. 수정: 그리드에서 브라우저 캐시에 이미 있는 **sm을 밑에 즉시 깔고** xl은 `onLoad` 후 페이드인(150ms 크로스페이드) + 로딩 스피너 + 실패 시 폴백 아이콘. 로딩 상태는 리셋 이펙트가 아니라 **src 파생 상태**(`xlState.src === xlSrc`)로 관리 — 사진 전환 시 이전 사진 잔상·경합 프레임 원천 차단. 핀치/팬 transform은 sm 레이어에도 동일 적용. Playwright로 xl 지연 시 sm(opacity 1)+xl(opacity 0)+스피너 상태 확인

### B-3. 다중 선택 (Google Photos 패턴)
- [x] 호버 시 좌상단 **체크 서클** — 별도 "선택 모드 버튼" 불필요
- [x] **사진 클릭=열기 / 체크 클릭=선택** 분리 유지(선택 모드에선 클릭=토글)
- [x] **Shift 호버 시 범위 프리뷰 하이라이트** — `store/timeline.ts` previewIds
- [x] 날짜 헤더 체크의 전체/부분 상태는 **선택 집합에서 파생**(이중 상태 관리 금지 — [Immich #17304](https://github.com/immich-app/immich/issues/17304) 버그 사례)
- [x] 작업 완료 시 선택 자동 해제 + Undo 토스트 (`useFileOps` afterOperation) + ESC/X 해제
- [x] 드래그 박스 선택은 벤치마크 초과 스펙(Google Photos·Immich에 없음) — `@air/react-drag-to-select` 좌표 기반, 배경 드래그만 시작(셀 드래그는 DnD)

### B-4. 드래그앤드롭 (Finder 관례 — 주 사용층 Mac)
- [x] 드래그 기본=**이동**, **Option(⌥)=복사** — 고스트에 모드 라이브 표시(실제 복사/이동 동작은 파일 작업 단계)
- [x] 고스트: 대표 썸네일 스택 + **"n장" 배지** (dnd-kit `DragOverlay` 커스텀)
- [x] 유효 드롭 하이라이트 + **무효 대상 안내** (2026-07-03) — 폴더 하이라이트는 기존 구현, 고스트 배지가 드롭 유효성 피드백을 겸함: 폴더 위 = 초록 "놓아서 이동/복사", 그 외 = 회색 "이동 — 폴더 위로" (`onDragOver` 추적 → DragOverlay)
- [x] **spring-loaded folders** (2026-07-03) — 드래그 중 접힌 대상 위 600ms 유지 시 자동 열림: 트리 노드는 펼침, 폴더 뷰 카드/리스트 행은 드릴인 (`SPRING_MS`, isOver는 드래그 중에만 참이라 일반 hover엔 무반응)
- [x] **드롭 확인 다이얼로그 금지** — 즉시 실행 + Undo (명세 원칙 그대로)

### B-5. 라이트박스 (Google Photos/Immich 표준)
- [x] 단축키: `←/→` 넘기기 · `i` 정보 패널 · `Delete` 휴지통 · `ESC` 닫기 · `Shift+?` 도움말
- [x] EXIF 패널은 **우측 슬라이드-인**(`i` 토글), 열림 상태는 다음 사진에도 유지 (하단 패널 비권고)
- [x] **삭제 시 닫지 않고 다음 사진으로 자동 전진** — 연속 정리(culling) 워크플로의 핵심
- [x] 다음/이전 이미지 프리페치
- [x] 라이트박스 내 "폴더로 이동" 버튼 — 삭제·이동(FolderPickerDialog) 버튼 구현됨
- [x] **정보 패널 상세화** (2026-07-02) — 기존엔 목록 필드만 표시(DSM 목록은 `folder=None` 하드코딩이라 폴더가 항상 "미지정", EXIF는 요청 자체 안 함). `GET /api/photos/item-detail`(Browse.Item `get` + additional exif/folder/address/gps, **패널 열릴 때만 온디맨드** 조회 — 목록 API는 가볍게 유지) 추가로 폴더 전체 경로 + 촬영 정보(카메라/렌즈/조리개/셔터/ISO/초점거리) + 위치(지오코딩 주소) 표시. **실 NAS 검증 완료(2026-07-02)**: 양 공간 폴더 경로·EXIF·주소 정상. **발견·수정**: DSM `additional.folder`는 dict가 아니라 경로 **문자열**로 옴(DSM 세션 raw 진단으로 확정) — dict/문자열 겸용 파싱 필요. 진단 부산물: DSM 웹 세션의 entry.cgi 호출은 SynoToken 없이는 success+빈 응답을 주므로 주의
- [x] **로컬 저장 위치 선택** (2026-07-21, 사용자 요청) — "저장" 버튼이 지원 브라우저(Chrome/Edge 데스크톱)에선 `showSaveFilePicker`(File System Access API)로 저장 위치·파일명 선택 후 `res.body.pipeTo(writable)` **스트리밍 저장**(큰 동영상도 메모리 무부담) + 완료/실패 토스트, 취소(AbortError)는 무음. 미지원(모바일 Chrome/Safari/Firefox)은 기존 앵커 다운로드 폴백(버튼 title로 구분 안내; Android는 Chrome "다운로드 위치 확인" 설정으로 매번 선택 가능). 유틸 `lib/saveFile.ts` 분리. 주의: showSaveFilePicker는 transient activation(클릭 제스처 내 호출) 필수, lib.dom에 타입이 없어 Window 선언 보강

### B-6. Undo / 확인 / 진행률 (NN/g + Gmail)
- [x] 가역 작업(이동/휴지통행 삭제)은 **확인 팝업 없이 Undo 토스트** ([NN/g Confirmation Dialogs](https://www.nngroup.com/articles/confirmation-dialog/)) — 이동/복사/삭제/폴더생성 전부 즉시 실행 + "되돌리기" 액션 토스트
- [x] 액션 버튼 있는 토스트는 **7–10초** 유지 + 호버 시 타이머 정지 (3초는 너무 짧음) — 8초 + 호버 정지(`Toasts.tsx`)
- [x] **작업 기록 패널이 토스트의 안전망** — OperationsPanel에 항목별 [되돌리기] 구현됨
- [x] **벌크 작업 개수 기반 진행 바** (2026-07-02, NN/g) — 이동/복사/삭제/되돌리기에 "34/120장 이동 중" 진행 바. 백엔드: CopyMove를 25개 청크로 분할 실행 + `progress_key`(클라 생성) 기반 인메모리 진행률 레지스트리(TTL 프룬) + `GET /api/ops/progress` 폴링 엔드포인트; undo(`_reverse`)는 항목 단위 보고(항목별 CopyMove라 가장 느린 경로). 프론트: `useFileOps`가 700ms 폴링→`BulkProgress` 바(하단 중앙, **1초 이상 걸릴 때만 표시** — 짧은 작업엔 안 뜸). mock은 실 NAS 벌크 지연을 시뮬레이션(10개당 0.1s)해 NAS 없이 바 확인 가능. e2e: 181장 삭제에서 "70/181장 삭제 중…" 렌더 확인. **주의**: `crypto.randomUUID`는 secure context 전용이라 HTTP 접속(NAS)용 fallback 키 생성 포함
- [x] **영구 삭제만 확인 다이얼로그 + 휴지통 비우기** (2026-07-02) — 작업 기록 패널에 휴지통 요약("N장 · M개 작업") + **비우기(관리자 전용** — 공유 휴지통이라 가족 전체 undo에 영향). 확인 다이얼로그에 "n장 영구 삭제" 명시 + 되돌릴 수 없음 경고. 백엔드: `purge_trash`(DSM: `/photo/#trash` 재귀 삭제, 다음 삭제 때 자동 재생성) + delete op들을 `purged` 상태로 전환(undo 409 차단) + `empty_trash` 감사 로그. `GET /api/ops/trash` 통계. pytest 3건 + mock e2e(다이얼로그→비우기→통계 0→undo 차단→감사 로그) 검증
- [x] 파괴적 버튼은 일반 버튼과 색·간격으로 분리 — 비우기 다이얼로그에서 취소(좌, 중립)/영구 삭제(우, 빨강) 분리 배치

### B-7. 관리자 모드 (impersonation 배너 표준)
- [x] "보는 중: ○○의 개인 폴더" 배너 — ImpersonationBanner 구현됨(주황 고정 + 원클릭 복귀)
- [x] 해당 모드의 모든 작업을 작업 기록에 **"관리자 수행"으로 명시** — OperationsPanel에 `대상: ○○ (관리자 수행)` 표기 구현됨
- [x] 타인 컨텍스트에서 고위험 작업(영구 삭제)은 **추가 확인** — 2026-07-11: 타인 열람 중 휴지통 비우기 차단(토스트 안내), 검색창도 타인 열람 중 숨김(→ 2026-07-25 B-8: 비활성+이유로 변경). 드래그 박스 선택은 **미구현 확정**(명세 §9 현행 IA 블록 참고)

### B-8. 메뉴 IA 개편 4단계 (2026-07-25, 사용자 요청 "처음 쓰는 사람도 이해하기 쉬운 구조")
> IA 리뷰 아티팩트(현재 구조·장단점·To-Be·계획) 기반. 골격(2축: 라이브러리×영역, 감상/정리 분리)은 유지, 표면만 수정.
- [x] **1단계 라벨**: 공용→가족, 1차 구역→기기 백업, 폴더 분류→정리, 폴더 렌즈→📂폴더별 보기(아이콘 중복 해소), 마법사 탭→정리 도우미, "2차" 전문용어 제거("내 사진으로"). 앨범 영역 셀렉터는 "내 사진" 고정 표시(죽은 컨트롤 방지). 내부 코드명(team/zone/manage)은 유지 — UI 문자열만
- [x] **2단계 더보기(⋯) 허브**: 4번째 영역(모바일 탭+데스크톱 토글 동일 목록). 휴지통 일급 승격(OperationsPanel `initialTrashOpen`으로 재사용), 작업 기록·기기 백업 관리(구 구역 관리, 셀렉터 드롭다운에서 이사)·DSM 정보·빌드 진단(BuildBadge→BuildDiag)·계정/로그아웃 수용. 헤더는 라이브러리·영역·검색·MOCK만. 더보기 진입은 기기백업/타인 선택을 풀지 않음(setSection 스냅백 예외)
- [x] **3단계 셀렉터 예측 가능성**: 기기 백업·구성원 항목 전 영역 상시 노출(+"정리에서 열림" 힌트), 감상 중 선택 시 예고 토스트 후 자동 이동, 감상 복귀 스냅백 시 "내 사진으로 돌아왔어요" 토스트, 검색창 숨김→비활성+이유 툴팁
- [x] **4단계 진입점·안내**: 정리 탭 순서 재편(폴더·정리 도우미·중복 정리 — 위계 표현, 중복 탭에 "도우미 1단계와 같은 기능" 툴팁), 검색 결과 컨텍스트 칩("검색어"+✕=history.back 복귀), 타임라인 빈 상태 다음 행동 안내, FirstRunTip(첫 로그인 1회, 2축+되돌리기 소개, localStorage)
- [x] **배포 전 전체 리뷰(2026-07-25, 직접+code-reviewer 독립 이중 리뷰)** — CRITICAL/HIGH/MED 회귀 0. LOW 수정 5건: ① 백엔드 에러 메시지 옛 용어 2건(zone_store 등록 검증·촬영일 교정 403), ② 더보기에서 일반 라이브러리 선택 시 no-op 데드엔드 → 사진으로 이동, ③ FirstRunTip localStorage 키를 계정별(`firstRunSeen.<account>`)로(한 기기 다계정 가족 대응, key=account로 재로그인 재평가), ④ saveFile: 피커 자체 실패(금지문자 등) 시 앵커 폴백 + HTTP 실패 시 피커가 만든 빈 파일 best-effort remove(), ⑤ mock MoveOutcome "내 사진(2차)"→"내 사진". **수용(의도 유지)**: 더보기에서 기기백업/타인 배너 잔존은 컨텍스트 보존 표시로 유지, 검색 칩 ✕의 history.back()은 useBackTrap 센티넬 동기화상 올바른 선택
- [x] **FirstRunTip 정합성·재열람** (2026-08-02, 안내 화면 검토 후속) — ① 첫 불릿의 라이브러리 목록을 계정 실제 메뉴와 동기화: 기기 백업은 계정별 등록제(zone_config WHERE account)라 처음 로그인한 구성원 메뉴엔 없음 → zones 조회로 있을 때만 표기(신규 일반 계정 실측: 「가족 사진 · 내 사진」 둘만). ② 배경 탭 실수 dismiss가 영구였던 문제 → 더보기에 "처음 안내 다시 보기"(💡, store firstRunTipTick 1회 재표시 — localStorage는 유지). ③ 탭 위치 반응형 표기("화면 아래/위 탭" — BottomTabBar md:hidden과 짝). ④ role=dialog/aria-modal/aria-labelledby. mock 실측: 신규 계정 문구·다시 보기·데스크톱 "위 탭" 전부 확인
- 남은 것(후속 후보): 검색 결과를 현재 영역 오버레이로(현재는 정리로 이동+칩), 기기 백업 빈 상태에 백업 앱 연동 안내, 코치마크 3장 확장

### B-9. photo_desk(Lap) 벤치마크 반영 (2026-09-01, 사용자 요청 "벤치마크하거나 적용하면 좋을 점")
> 맥의 photo_desk 는 «Lap»이라는 앱의 UI 를 포팅한 것이라(코드 주석에 대응 관계가
> 적혀 있다) 두 앱을 함께 살폈다. 감상 쪽에 값이 있는 것만 가져오고, 정리 계열
> (SelectMenu·Cull·Compare)은 ECOSYSTEM 6절의 선을 지켜 가져오지 않았다.
- [x] **늘 보이는 가는 스크롤바(`.scroll-thin`)** — 겹침 스크롤바는 손을 떼면 사라져 «스크롤할 게 있다»는 것조차 안 보인다. 스크러버가 없는 목록(앨범·검색·폴더별 보기·더보기·작업 기록·기기 백업 관리·정보 패널)에 적용. 스크러버가 붙는 곳에는 달지 않음(레일과 겹침)
- [x] **날짜 스크러버를 앨범·렌즈 그리드로 확장** — 기존 `Scrubber` 재사용. 붙이기 전 `isMonthOrdered`로 목록이 달 순서인지 확인(앨범=큐레이션 순서엔 안 붙임 — 레일이 거짓말을 한다). **판정은 달까지만** — 실제 목록은 같은 날 안에서 시각이 뒤섞여 온다(실측 `08:32 → 17:05 → 14:18`), 초 단위로 따지면 멀쩡한 목록이 전부 탈락
- [x] **메이슨리 배치**(정사각 ↔ 비율 토글, localStorage 영속) — 정사각 크롭이 세로 사진의 얼굴을 자르는 문제. 행이 없어 행 가상화를 못 쓰므로 배치를 600px 띠로 색인해 보이는 띠만 렌더(`lib/masonry`)
- [x] **동영상 호버 미리보기** — 400ms 지연 + **재생권 전역 1개**(Lap 의 hoverPreview 잠금). NAS Range 프록시라 동시 재생이 커넥션 풀까지 먹는다(F절 PoolTimeout 과 같은 길). 터치·reduced-motion 제외
- [x] **라이트박스 필름스트립**(`f` 토글, sm+ 기본 표시) — store 의 `orderedIds` 를 그대로 봐서 정렬·필터가 저절로 맞음. 가로 가상화
- [x] **회귀 발견·수정: 사람·장소·비디오 렌즈의 행 가상화가 죽어 있었다** — `TagLensBody` 의 `min-h-0 flex-1` 은 부모가 flex 상자가 아니라 무시되고 높이가 콘텐츠만큼 늘어난다 → 스크롤 상자의 «보이는 높이»가 곧 전체 높이가 되어 가상화가 통째로 풀림. 318장 인물에서 **318장 전량 마운트**(실측) → `h-full` 로 바로잡아 136장. 656장 장소는 132장
- **가져오지 않은 것**: `Calendar`(연·월·일 트리) — 뷰어 줌(연/월/일)과 중복. `gridStyle` 4종 중 카드·타일 — 감상에 이름줄이 필요 없고 타일은 정사각과 겹침
- 검증: MOCK + Playwright 로 사람·장소·앨범 3화면 실측(높이 사슬·가상화 렌더 수·레일 표시·scroll-thin 배타 적용·필름스트립 `f` 토글·좌우 넘김 추종), 콘솔 에러 0
- 남은 것(후속 후보): `SearchView`·`FolderViewerGrid` 의 스크러버 — justified 가변 행 + 그리드 위 헤더 구조라 마커의 픽셀 위치를 바깥에서 알 수 없다(스크롤 오프셋을 `VirtualRows` 내부가 소유). 두 목록 모두 시간순 보장도 없어 지금은 `.scroll-thin` 으로만 처리
- [x] **배포 후 발견: 두 기능을 만날 수조차 없었다** (2026-09-01, 사용자 제보 "필름스트립, 메이슨뷰 이런건 전혀 안보이는데?") — ① 필름스트립을 `hidden sm:block` 으로 넓은 화면에만 뒀는데 주 사용 기기가 폰이었다(여는 수단도 `f` 키뿐). 폰에서도 펴고 상단에 🎞 단추 추가. ② 배치 전환 단추를 그리드 좌상단에 띄웠는데 가장 많이 보는 「사진」 탭(타임라인)은 그 그리드를 쓰지 않는다 → 렌즈 바·앨범 머리줄의 늘 같은 자리로 이동
- [x] **타임라인에 비율 보기** — 그룹별 지연 로드 + 행 가상화라 열 기반 메이슨리는 넣을 수 없다(그룹이 채워질 때마다 배치가 통째로 흔들림). justified 로 **행 구조를 지키면서** 크롭만 없앴다(`layoutBucket` 재사용 + `targetRowHeight` 인자 추가). 배치 후 «행 수»로 잘라 연 줌 미리보기 줄 수를 일정하게 유지

---

## C. 프론트 스택 확정 + 구현 함정

| 역할 | 라이브러리 | 주의점 |
|---|---|---|
| justified 계산 | `flickr/justified-layout` | 순수 계산 라이브러리(렌더 무관). 가상화 단위는 사진 1장이 아니라 **justified 행/일(day) 섹션** |
| 가상 스크롤 | `@tanstack/react-virtual` | [공식 sticky 예제](https://tanstack.com/virtual/v3/docs/framework/react/examples/sticky)가 날짜 헤더 패턴 그대로. 행 높이를 `estimateSize`에 사전 주입 |
| DnD | `@dnd-kit/core` | **멀티 드래그 네이티브 미지원**([#120](https://github.com/clauderic/dnd-kit/issues/120)) → 선택 상태 + `DragOverlay` 커스텀. **가상 리스트에선 DragOverlay 필수**(원본 언마운트 대비). 폴더 드롭은 `useDroppable`만으로 충분 — SortableContext 트리 구현 금지. **터치 주의(2026-07-03 실기기 발견)**: PointerSensor 하나로 쓰면 터치 스크롤이 8px 이동 드래그로 잡혀 onDragStart의 선택 규칙이 오발동 → MouseSensor(distance 8) + TouchSensor(**delay 300ms + tolerance 8** = 길게 눌러야 드래그) 분리 + 셀에 `-webkit-touch-callout: none`(iOS 저장 시트 충돌 방지) |
| 드래그 박스 선택 | `@air/react-drag-to-select` | 좌표만 넘겨주는 설계 → 가상화와 호환(좌표→그리드 인덱스 역산). DnD와의 충돌은 PointerSensor `activationConstraint.distance`로 분리(빈 영역=박스 선택, 썸네일 위=드래그) |
| 블러 플레이스홀더 | `thumbhash` | BlurHash보다 작고 디테일 좋음 |

**피해야 할 함정 3가지** — 전부 회피 구현됨(2026-07-02)
1. 큰 버킷 geometry 동기 일괄 계산 → 브라우저 프리즈 (Immich #28861) → **일 단위 버킷 + 버킷별 지연 계산·메모이제이션**(`lib/rowModel.ts`)
2. dnd-kit 멀티 드래그를 기본 기능으로 착각 / DragOverlay 없이 가상 리스트에서 드래그 → **선택 스냅샷 + DragOverlay 커스텀 고스트**(`TimelineScreen.tsx`)
3. 드래그 박스 선택과 DnD 드래그 시작 충돌 → **activation distance 8px + 시작점 분기**(셀=DnD, 배경=박스 선택, `shouldStartSelecting`)

**v1 한계 해소 현황(2026-07-02)**:
- ~~위쪽 버킷 로드 시 스크롤 밀림~~ → **스크롤 앵커링 구현**(첫 가시 행의 날짜 헤더 키에 앵커, useLayoutEffect에서 오프셋 델타 보정 — Google Photos 기법)
- ~~드래그 중 엣지 자동 스크롤~~ → dnd-kit **기본 autoScroll**이 스크롤 가능 조상을 자동 감지(별도 코드 불필요 확인)
- ~~라이트박스 이동 버튼~~ → 폴더 피커 연결 구현
- ~~spring-loaded 폴더~~ → 폴더 뷰 목록에서 드래그 호버 600ms 시 자동 열림
- (유지) 박스 선택은 화면에 마운트된 셀만 대상 — 가상화 특성상 의도된 트레이드오프

---

## D. 2단계(중복 제거) 설계 확정 — **구현 완료 (2026-07-02)**

명세 11장 방향(체크섬 + imagehash)이 벤치마크로 검증됨. 구체화:

- [x] **2단계 탐지**: SHA-256(정확 중복) + **pHash 64bit**(near-duplicate, **Hamming ≤ 5** 기본 — 0~2 사실상 동일, ~5 유사, 10+ 다른 사진). 임계값은 사용자 조절 슬라이더(0~7)로 노출(Czkawka 패턴)
  - **편차 기록**: `imagehash` 패키지 대신 **자체 pHash 구현**(`photos/hashing.py`, Pillow만 의존) — imagehash는 numpy+scipy를 끌고 와 NAS 컨테이너가 무거워짐. 알고리즘 동일(32×32 그레이스케일→DCT-II→8×8 저주파→중앙값 비트), 실이미지 단위 테스트로 검증(재압축=지각적 동일, 소규모 편집=near, 구조 다름=far). DSM 스캔 규모에서 필요 시 numpy 전환
- [x] 해시는 **Synology 썸네일 다운로드본에 계산**(원본 전송 회피) — DSM 소스는 sm 썸네일 bytes로 실계산(실 NAS 검증 항목: 동일 원본→동일 썸네일 전제), mock은 중복 클러스터를 심은 결정적 시뮬레이션(복사본은 원본 해시 상속 → 복사→스캔→정확 중복 검출 데모)
- [x] `photo_cache`에 `sha256`, `phash` 컬럼 추가 + `taken_at`/`space` 인덱스 — **해시 영속화로 재스캔 시 재계산 회피**(중단된 스캔도 자연 재개)
- [x] 전수 쌍 비교(O(n²), dupeGuru 반면교사) 대신 **멀티 인덱스 버킷팅**(8밴드×8비트 — 비둘기집으로 Hamming ≤ 7 재현율 보장) + union-find
- [x] **잡 처리**: Redis 없이 **SQLite `job` 테이블 + asyncio 워커 태스크**. 진행률(processed/total) 폴링, 취소, 서버 재시작 시 running→failed 전환+해시 영속화 기반 재개. (썸네일은 Synology 재활용이라 스캔→해시 단일 단계로 충분 — 체이닝 불필요)
- [x] **UX**: 그룹 목록 자체가 dry-run(삭제 예정 표시 + 절약 용량) → 그룹별 **기준(reference) 파일 자동 선택**(해상도→용량→촬영일, 클릭으로 뒤집기 — dupeGuru 패턴) → 기존 삭제 플로우 재사용(휴지통 + 작업로그 + **되돌리기 토스트**). 그룹은 절약량 상위 N개 페이지네이션(수천 카드 렌더 방지)
- [x] **중복 그룹 카드 비교성 강화** (2026-07-03, 사용자 요청) — ① 각 사진에 **폴더 경로** 표시: photo_cache가 파일명만 저장하므로 groups 응답 시 상위 N개 그룹 아이템만 `item_folders`(Browse.Item get 배치, ~100개/콜)로 온디맨드 해석(스캔 재실행 불필요, 실패 시 폴더만 공백). ② **사진 클릭 = 라이트박스 크게 비교**(그룹 사진들만 ←/→ 순회, `i` EXIF/폴더, Delete 휴지통) — 보관본 변경은 별도 [보관] 배지 클릭으로 분리(클릭 충돌 해소). ③ 카드에 파일명·해상도·용량·**촬영일**·폴더 메타 표기. ④ **멀티 보관**(2026-07-03): 그룹당 보관본 1장 → **보관 집합**으로 확장 — 배지 클릭으로 장별 보관↔삭제 토글(연속촬영 그룹에서 여러 장 보관), 최소 1장 보관 강제, 전부 보관 시 정리 버튼 비활성
- [x] **사용성 라운드 2** (2026-07-03, 사용자 요청 4종): ① **사진 검색** — 헤더 검색창 + 검색 뷰, DSM `Search.Search list_item v1`(실 NAS raw 검증: 한국어·폴더명 매칭, 폴더 검색 `list_folder`는 미지원(103) 확인) ② **모바일** — 헤더 flex-wrap 2행·아이콘화·부차 요소 숨김(390px 오버플로 837→0), 액션바 max-w+wrap. **2차(2026-07-03)**: 라이트박스 **좌우 스와이프**(이전/다음)·**아래로 쓸어 닫기**(비디오 컨트롤 터치는 제외, 화살표 버튼은 sm 이상 전용), 폴더 뷰 좌측 트리 모바일 숨김(390px에서 화면 60% 차지하던 문제 — 카드 탐색으로 대체). **3차(같은 날)**: 라이트박스 **핀치 줌(1~4x)·더블탭 확대(2.5x 토글)·확대 중 한 손가락 팬**, 확대 중엔 스와이프 넘김 비활성, 사진 전환 시 리셋. **4차(같은 날)**: 모바일 **하단 탭 바** — 뷰 전환 4탭을 하단 고정(엄지 도달성, safe-area 대응), 헤더 뷰 토글은 md 이상 전용, 액션바/진행바/토스트는 탭바 위로 재배치
- [x] **폴더 생성/삭제 (분할 뷰 요청)** (2026-07-03) — FolderPane 브레드크럼에 [＋새 폴더]/[폴더 삭제] 통합(단일·분할 공용, 좌측 트리 버튼 제거). 생성: 현재 위치 하위(`Browse.Folder create + target_id` — 실 NAS raw 검증, Foto id 즉시 반환), 루트면 라이브러리 최상위. 삭제: **빈 폴더만**(`remove_folder`에 사진/하위 폴더 검사 추가 — 기존엔 검사 없이 통째 삭제라 위험했음), 비어있지 않으면 409, 작업로그 `rmdir`(undo 불필요 — 손실 없음). 타인 개인 공간(경로 id)도 FileStation 경유 지원 ③ **폴더 피커 최근 사용** — 확정 시 localStorage 기록(6개), 칩 원클릭 선택 ④ **중복정리 편의** — [정확 중복만] 필터(유사=연속촬영 오판 배제, 일괄 정리 안전) + [그룹 더 보기] 100→500 페이지네이션
- [x] **동영상 지원** (2026-07-03, 사용자 요청) — 기존엔 정지 썸네일로만 표시. ① 목록/중복 카드에 **▶+길이 배지**(`Browse.Item` additional `video_meta.duration`, item `type` 필드 — 실 NAS raw 검증) ② **라이트박스 재생**: `<video controls>` + `GET /api/photos/video` 프록시 — DSM `SYNO.Foto(Team).Download`가 **Range 요청에 206+Content-Range 응답**(실측)이라 시킹까지 패스스루(`DsmClient.stream_binary`, 64KB 청크, 메모리 비적재). 타인 개인 공간(경로 id)은 `FileStation.Download`로 스트리밍 ③ 정보 패널에 길이. mock은 1/19 확률 video 아이템(재생은 404 안내). **주의: NPM 프록시 뒤에서 대용량 영상 시킹은 proxy_read_timeout 600s 설정 전제**
- [x] **폴더째 이동/복사 (분할 뷰)** (2026-07-04, 사용자 요청) — 사진 단위가 아닌 **폴더 단위**(하위 전체 포함) 정리. ① 백엔드: `POST /api/photos/ops/move-folders`(`folder_ids[]` 최대 100 + `dest_folder_id`), DSM은 FileStation `CopyMove`가 디렉터리 재귀 이동/복사를 지원(2026-07-03 MobileBackup 평탄화에서 실사용 검증)하므로 `_copymove_chunked` 재사용 + **자기 자신/자기 하위로 이동 가드**(경로 prefix 검사, 409) + 이미 대상 안이면 no-op skip(전부 skip이면 409). ② **undo 지원**: 작업로그 `move_folder`/`copy_folder` — 이동 취소는 역방향 CopyMove, 복사 취소는 사본 폴더 영구 삭제. ③ 프론트: 분할 뷰 폴더 카드/행에 **✓ 체크 서클**(단일 뷰엔 미표시), 체크 시 가운데 액션바가 `📁n개` 폴더 모드로 전환(이동/복사 활성, 삭제는 비활성 — 폴더 삭제는 브레드크럼 [폴더 삭제] 유지). 사진 선택과 배타적(한쪽 선택 시 반대쪽 해제), 페인 전환/탐색 시 해제(사진 선택과 동일 규칙). 폴더 구조가 통째로 바뀌므로 folders/folder-items/folder-counts/buckets 광역 무효화. mock: flat 구조라 이름 리네임으로 시뮬레이션. 검증: pytest 82(신규 4) + mock UI e2e(체크→이동→토스트→작업기록 undo→원상복구). **실 NAS 검증 대기**
- [x] **폴더 뷰 사진 모두 선택/해제** (2026-07-04, 사용자 요청) — FolderPane 사진 섹션 헤더 우측에 토글 버튼: 현재 폴더 사진 전체 선택↔해제, 부분 선택 시 `–` 표시. DateHeader와 동일한 파생 상태 패턴(선택 집합에서 매 렌더 계산 — B-3 이중 장부 금지) + `setMany` 재사용. 단일·분할 공용(분할에선 클릭한 페인이 활성화되며 그 페인 기준으로 동작). mock UI 검증(선택→액션바 3장→해제→부분 `–`)
- [x] **정리 워크플로우(마법사)** — **Phase 0~3 구현·배포 완료(fb1db04, 2026-07-12)**. 미결 확정: 처분 폴더 `_정리/<사유>`·장소 힌트 v1 제외·최소 8장 슬라이더·진입 `✨ 정리`(관리 탭). no_exif 룰은 photo_cache.camera 전 행 NULL이라 v1 제외. 실 NAS 검증: 후보 755장(스크린샷 724·메신저 31)·이벤트 제안 562건·세션 이어하기/리셋. 잔여 개선 3종도 완료(f33b55f·9001a12, 2026-07-12): ⑴ 이벤트 목록 30개 증분 렌더(+더 보기), ⑵ '이미 복사됨' 제외 — organize_copied 아이템 단위 기록(POST /api/organize/copied, 앨범 생성 시)·90%↑ 복사 클러스터 숨김+토글(검증: 기록→제안 562→561·copied_count 1948/1948·토글 복귀), ⑶ 장소 힌트 — 대표 3샘플(중간/처음/끝) item_detail의 **place_label**(원시 주소 필드 town>city>county>state>country, 2744d5c)로 도시급 라벨(예: 'Sydney', 'Hwaseong-si', 'Wentworth Falls'), GPS 없으면 생략. **최종 실측: 제안 198건 중 앞 40개에 라벨 37개, 1.0s.** 부수 수정: **dedup 스캔 완주 시 photo_cache 낡은 행 청소**(개인→공용 대량 이동 후 죽은 personal id 잔존이 제안·힌트를 오염시키던 문제). (원 설계: — 4기기 유입 개인 사진을 중복→잡동사니→이벤트 공용 앨범화로 정리하는 반복 가능 흐름. `docs/ORGANIZE_WIZARD.md`)
- [x] **파일명 충돌 처리 — 모든 이동 경로 (2026-07-05, 사용자 요청)**. 대상 폴더에 같은 파일명이 있으면 경고 다이얼로그로 처리 방법 선택. **초판은 분할 뷰 버튼에만 프리플라이트를 붙였으나**, 드래그·액션바·라이트박스 이동은 여전히 조용히 건너뛰어(overwrite=false skip) "이동했다는데 실제로 안 됨" 증상 발생 → **백엔드 강제 방식으로 재설계**: `MoveRequest.conflict_strategy` 기본값 `ask` → 충돌 있으면 `execute_move`가 **409 + 충돌 목록**(`{code:filename_conflict, conflicts:[{item_id,filename}]}`) 반환. 프론트는 `moveMutation.onError`에서 이를 감지해 **전역 `useConflictStore` + `ConflictDialogHost`**(앱 루트)로 다이얼로그를 띄우고 선택 전략으로 재시도 → **어떤 이동 경로든 동일하게 커버**. 3전략: **rename**(`_1`/`_2` 붙여 둘 다 보관 — DSM은 CopyMove가 타깃 리네임 불가라 temp 폴더 경유 copy→rename→move), **skip**(충돌만 제외), **overwrite**(비가역 경고). mock 시뮬레이션(파일명 override, extras 포함) + pytest(ask→409/skip/rename/overwrite/no-conflict-proceed). mock UI e2e: 액션바 이동 경로에서 409→전역 다이얼로그→rename→대상에 원본+`_1`, 원본 폴더 비워짐. HomesPhotoSource는 move 상속. `/ops/move-check` 엔드포인트는 잔존(진단용). **남은 것**: 실 NAS 검증
- [x] **폴더 이동 충돌 처리 (2026-07-05, 사용자 요청)** — 폴더째 이동 시 대상 위치에 같은 이름 폴더가 있으면 CopyMove가 overwrite=false로 **조용히 건너뛰고 성공 보고**해 "이동했다는데 아무 변화 없음" 발생. 사진과 동일한 강제 패턴으로 해결: `MoveFoldersRequest.conflict_strategy` 기본 `ask` → `execute_move_folders`가 `folder_conflicts()`로 감지해 **409+목록**(`{code:folder_conflict, conflicts:[{folder_id,name}]}`). 프론트 `moveFoldersMutation.onError`가 전역 `ConflictDialogHost`(파일/폴더 겸용으로 일반화)를 띄우고 선택 전략으로 재시도. 폴더 전략은 **merge/rename/skip**(overwrite는 전체 트리 소실이라 제외). `_list_subdir_names`로 대상 하위 폴더명 조회. mock 시뮬레이션 + pytest(ask→409/rename) + mock UI e2e(분할 뷰 폴더 이동→409→다이얼로그→`가족앨범/행사_1`). **남은 것**: 실 NAS 검증.
- [x] **폴더 merge(내용 합치기) + 이동 후 빈 폴더 정리 (2026-07-05, 사용자 요청)** — 폴더 충돌 다이얼로그에 **합치기(merge)** 옵션 추가: 대상의 동명 폴더 안으로 소스의 사진·하위 폴더를 재귀적으로 이동/복사. **내부 파일명 충돌은 건너뛰기**(기존 파일 유지, 소스 것은 남김 — 사용자 결정), 같은 이름 하위 폴더는 재귀 merge, 비충돌 하위 폴더는 통째 이동. 이동 모드 merge는 병합 완료 후 비워진 소스 폴더를 삭제(undo 복원 가능). 백엔드: `FolderConflictStrategy += merge`, `DsmPhotoSource._merge_dir`(재귀 — `_list_entries`/`_copymove`/`_ensure_dir` 조합, undo payload `{kind:merge|merge_copy, moved/created/children}`), `revert_move_folders`를 원소별 kind 디스패치(`_revert_folder_entry`)로 재작성해 통째 이동/복사와 merge를 한 작업에 혼합 가능. **이동 후 정리**: 사진 이동(move, not copy)이 원본 폴더를 완전히 비우면 `MoveOutcome.emptied`(folder_id·basename·space) → `OperationResponse.emptied_folders`로 반환. 프론트 `useFileOps.offerCleanup`이 **"빈 폴더 정리" 액션 토스트**를 띄워(자동 삭제 아님) 원클릭 정리(rmdir 경로 재사용 — 비지 않은 폴더는 거부하므로 지연 도착 파일 손실 없음). `_item_meta`가 원본 folder_id를 메타캐시에 채워 타임라인 이동도 경로 해석 가능. `_is_dir_empty` 헬퍼 추출(remove_folder와 공용). mock: flat 구조라 직속 사진만 병합 시뮬 + emptied 감지. pytest 5건 추가(merge 병합/undo/emptied 보고/복사는 정리 없음). **남은 것**: 실 NAS 검증(merge 재귀 이동·소스 삭제·undo, 사진 이동 빈 폴더 감지).
- [x] **재귀 폴더 삭제 + 폴더 뷰 삭제/이동 인덱스 지연 보정 (2026-07-05, 사용자 요청 3종)** — ① **재귀 폴더 삭제**: 기존엔 빈 폴더만 삭제(사진 있으면 409). 이제 폴더 뷰 [폴더 삭제]가 비어 있으면 바로, 내용이 있으면 **확인 다이얼로그 후 통째로 휴지통 이동(가역)**. `RemoveFolderRequest.recursive`, `PhotoSource.trash_folder`(DSM은 폴더를 `/photo/#trash/t<ns>/`로 CopyMove 한 번에 서브트리째 이동, undo는 `restore_folder`로 원위치 역이동; mock은 직속 사진 휴지통+폴더 제거), op type `trash_folder`(7일 undo 기한). 프론트는 화면에 이미 로드된 직속 사진 수+하위 폴더 수로 빈 폴더 판단. ② **삭제 후 사진이 폴더에 그대로 남아 보이던 문제** — Photos 인덱스가 `#trash` 이동을 재색인하기 전까지 folder_id 필터·타임라인이 삭제/이동 아이템을 계속 반환(trash 필터도 재색인 후에야 잡음). **item tombstone** 도입(`_REMOVED_ITEMS` sid→{item_id:ts}, 폴더 tombstone과 동형): `delete`/`move`가 삭제·이동 id를 즉시 tombstone → `items`/`_filtered_items`(folder_items)/`buckets`/`search`에서 제외, undo(place/restore)가 해제. ③ **폴더 뷰에서 사진 한 장 이동이 실패하던 문제** — folder_items가 재색인으로 바뀐 stale id를 반환해 `_item_meta`가 경로를 못 찾던 케이스: tombstone으로 목록을 신선하게 유지 + `useFileOps`의 지연 재조회(resettle)에 `folder-items` 추가(이동/삭제 후 새 위치가 재색인되는 대로 수렴). **검증**: mock UI e2e(가족앨범 2장 → 폴더 삭제 → 확인 다이얼로그 → 휴지통 이동 토스트 → undo로 2장 복원), pytest 114개(재귀삭제/undo, item tombstone folder_items 필터·해제, delete tombstone 등록). **남은 것**: 실 NAS 검증(tombstone 유효기간 내 재색인 catch-up, 재귀 폴더 CopyMove·복원).
- [x] **휴지통 필터가 중첩 트래시를 놓치던 문제 (2026-07-06, 사용자 보고: 사진 뷰에 휴지통 사진 노출)** — `_trash_item_ids`가 `#trash/t<ns>/files` **2단계**만 훑어 직속 아이템만 수집했다. 하지만 재귀 폴더 삭제(`trash_folder`)는 폴더를 통째로 `#trash/t<ns>/<folder>/…`로 옮겨 **임의 깊이**가 되므로, 그 안에 중첩된 사진들이 필터에서 누락되어 재색인 후 team 타임라인/뷰어에 계속 노출됐다(tombstone은 재시작 시 소실 → 재배포 후 재발). `_descendant_folder_ids`(BFS로 `#trash` 서브트리 전체 폴더 id 수집) 추가 후 그 폴더 전부의 아이템을 모아 제외. `_trash_item_ids`를 공유하는 `buckets`/`items`/`search`/`persons`/`places`/`folder_items`가 모두 함께 교정됨. **검증**: pytest 125(중첩 트래시 3단계 수집 신규 `test_trash_item_ids_recurses_nested_folder_deletes` + 기존 2단계 유지). **남은 것**: 실 NAS 검증(폴더 삭제→재색인 후 뷰어에서 사라지는지).
- [x] **폴더 완전 일치 감지 + 원본 정리 / 추가분만 이동·복사 (2026-07-05, 사용자 요청)** — 폴더 이동/복사 시 대상에 같은 이름 폴더가 있으면, 두 폴더를 **재귀 비교(파일명+크기)** 해 원본이 대상에 얼마나 포함되는지 판정. `folder_conflicts`가 각 충돌에 `extra_count`(원본이 대상에 없는 항목 수, 재귀·하위 폴더 포함)를 실어 409로 반환(`DsmPhotoSource._folder_extra_count`, `_list_entries`에 size additional 추가; mock은 직속 사진 파일명 비교). **UX 분기**(`ConflictDialog`): ① **완전 일치**(모든 원본 extra_count 0) — 이동이면 "내용이 대상과 완전히 같습니다 → **원본 폴더 삭제(휴지통, 되돌리기 가능)**/그대로 두기", 복사면 "이미 동일 — 복사할 것 없음" 안내. ② **부분 차이** — "합치기(대상에 없는 약 N개만 이동/복사)"로 추가분만 병합(기존 merge; **이 동작은 이미 되던 것 — 사용자 요청대로 확인함**). 완전 일치 이동의 원본 삭제는 **선택한 여러 폴더를 순차로** `trash_folder`(재귀 삭제 경로 재사용, FileStation 태스크 비중첩). 여러 폴더 이동/복사 자체는 `move_folders`가 `folder_ids` 배열을 순차 처리(기존). 프론트 `conflictInfoOf`가 extra_count 파싱→`ConflictDialog`가 전량 0이면 완전일치 UI. pytest 2건(부분차이 extra_count>0 / 완전일치 0) + 실 mock 서버 HTTP 검증(부분차이 merge 3장, 완전일치 원본삭제 1→0·대상 유지). **남은 것**: 실 NAS 검증(재귀 크기 비교, 하위 폴더 포함 완전일치).
- [x] **1차 구역(기기 백업 zone) — 3구역 워크플로 (2026-07-06, 사용자 요청)** — 개인 타임라인이 개인 공간(/homes/<user>/Photos) **전체**를 올려 기기 백업 원본까지 다 나오는 문제. 3구역 모델: **1차**=기기 백업(Photos 밖, 예 `/homes/<user>/MobileBackup` → 타임라인 제외) → **2차**=이벤트별로 골라 Photos 안으로 이동(타임라인 등장) → **3차**=공용. 앱은 1차를 **FileStation으로 읽어 폴더 탐색 뷰**로 보여주고(촬영일 EXIF 없어 날짜 타임라인 대신 폴더), 사진을 골라 2차로 이동/복사. **설계 핵심**: 기존 임퍼소네이션(`HomesPhotoSource`)의 경로형-id FileStation 파이프라인을 `_FsRootMixin`으로 추출해 재사용 — `ZonePhotoSource(_FsRootMixin+DsmPhotoSource)`는 개인/공용 Foto prefix를 **하이재킹하지 않아**(함정 A) 2차 목적지(개인 Photos)가 올바르게 계산되고, `folders(None)`은 **zone 루트 하위만** 반환해(함정 B) 프론트 루트 필터가 1차만 보여준다. `zone_config` 테이블(사용자별, 경로 화이트리스트 `/homes/<account>` 또는 `/photo`), `api/zones.py`(CRUD+browse), `get_photo_source`에 `?zone=` 분기(target_user와 배타 422, 미등록 404). 프론트: 스토어 `activeZone` 축, `scopeQS`(zone/owner 통합)·`destFolders`(스코프 미부착 2차 목적지), 라이브러리 셀렉터 "📦 1차 구역" + 인디고 배너 + `ZoneManager` 모달(폴더 탐색 등록), `FolderPickerDialog` `toPersonal` 모드, 액션바 "2차로 보내기". 재인덱싱 지연은 기존 tombstone+resettle 재사용. **검증**: pytest 123(zone CRUD/경로검증/브라우즈/이동·undo 7건) + **mock UI e2e 전 흐름**(구역 등록→1차 탐색→사진 선택→2차 이동→zone에서 사라짐). **남은 것**: 실 NAS 검증(사용자가 백업 경로를 Photos 밖으로 옮긴 뒤 — 데이터 이동은 사용자가 직접).
- [x] **UI 3영역 전면 개편 — 감상/정리 분리 (2026-07-06, 사용자 요청)** — 뷰 5종 평면(timeline/folders/dedup/classify/search)이 감상과 정리를 뒤섞어 UI가 제각각이던 문제. Synology Photos식 뷰어 + 우리 강점(폴더 정리)을 분리해 3영역으로: **① 사진(뷰어)** 공용/내사진, 연/월/일/폴더 줌(연·월=대표 커버 카드 그리드→드릴다운, 일=균일 정사각 가상화 그리드), 순수 뷰어(선택/이동/삭제 없음, 라이트박스는 정보+좌우만). **② 앨범** 사람/장소(기존 AI 그룹)/비디오(신규), 순수 뷰어. **③ 폴더 분류** 공용/내사진/1차백업/타인, 이동·복사·삭제 + 중복정리 통합(정리 전용 축은 여기서만). **구조**: 스토어 `section`(viewer/albums/manage)+영역별 서브상태(ViewerZoom/AlbumKind/ManageTab), `selectLibrary`/`setSection` 재작성(타인·zone은 manage 전용, 스냅백). 연/월 집계는 기존 `buckets`(day별 count) 프론트 롤업(`lib/rollup.ts`, 백엔드 0). `Lightbox`는 `section!=="manage"`면 readonly(이동/삭제/Delete 차단). `Thumb`/`UniformPhotoGrid` 추출로 뷰어/앨범 공유. 유일한 백엔드 추가는 `GET /api/photos/videos`(buckets 패턴 복제+type 필터). 기존 폴더/dedup/zone/useFileOps는 내부 수정 없이 manage로 재배치(무회귀). ClassifyView/TimelineView 제거. **검증**: 3 Phase 독립 배포(골격→사진뷰어→앨범), 각 mock UI e2e(연→월→일→readonly 라이트박스, 앨범 사람/비디오, 폴더분류 회귀), pytest 124(videos 1건 신규), 빌드 통과. **후속**: 태그·최근추가·앨범생성(실 NAS GeneralTag/추가일 sort 프로브 선행), PhotoCell을 Thumb 공유로 리팩터. (뷰어 연/월/일/폴더 줌 재설계는 아래 항목에서 완료.)
- [x] **사진 뷰어 연/월/일 썸네일 바둑판 + 폴더 뷰 드릴인 (2026-07-06, 사용자 요청)** — 위 3영역 개편의 뷰어를 사용자 피드백으로 재설계. 연·월을 **대표 커버 카드 그리드에서 일 뷰처럼 작은 썸네일 바둑판으로 통일**(`GroupedPhotoGrid` 신규 — 그룹 헤더 + 행 가상화 `@tanstack/react-virtual`): **연=작은 타일(minTile 58px) 10줄 미리보기**(초과분 "n장 더" 안내), **월·일=해당 월/일 사진 전부**(maxRows 무제한, 행 가상화로 수천 장도 렌더). 타일 크기는 `minTile` prop으로 뷰별 지정(연은 절반), 5줄 캡은 "가시 그룹 기준으로 미리보기 day를 계속 요청"해 cols 확대 후에도 채움(초기 width=0→cols=1로 적게 요청 후 안 채워지던 버그 수정). **드릴/열기**: 연 뷰 사진 탭 → 그 사진의 월로(전체 월 연속 스크롤에서 대상 월로 scrollToGroup — 가상화 동적 높이 오차는 대상 월 선요청 + 로드 완료까지 재스크롤로 보정), 월·일 탭 → readonly 라이트박스. **폴더 뷰 신규 구현**(`FolderViewerGrid`, 기존 placeholder 대체): 현재 폴더 구조를 그대로 한 depth씩 탐색 — 폴더 카드에 직속 사진 **4장 2×2 미리보기**, 리프 폴더 사진은 썸네일 그리드 → 탭 시 라이트박스, 브레드크럼 상위 이동. 중간 폴더(하위폴더만·직속 사진 0)는 📁 표시 + "하위 폴더를 열어 보세요" 안내(line 184 동작 노트 준수). **구현 함정**: ① zoom 3종이 같은 `GroupedPhotoGrid` 타입이라 React가 인스턴스 재사용 → zoom별 `key`로 remount(stale requestedDays/스크롤 누수 차단). ② `requestedDays` 중복 제거로 React Query "Duplicate Queries" 경고 해소. ③ mock `folders(parent_id)`는 자식 반환·`folders(없음)`은 기존 flat 계약 유지(manage 트리/pytest 의존) → 뷰어 루트는 `parent_id==null`로 자체 필터. **검증**: mock UI e2e(연→월 드릴 스크롤 위치, 월/일 readonly 라이트박스, 폴더 루트→중첩→리프→라이트박스 전체 드릴인), pytest 124 그린, 빌드 통과. mock에 테스트 미참조 데모 폴더(`f-demo-*`, 중첩+리프 사진 시드, 과거 날짜 배정으로 `_first_day_items` 무오염) 추가.
- [x] **뷰어 사이드 스크러버 복원 + 장소 지역별 뷰 (2026-07-06, 사용자 요청)** — ① **사이드 스크러버**: 3영역 개편 때 TimelineView와 함께 빠졌던 우측 날짜 스크러버(연도·월)를 `GroupedPhotoGrid`에 재연결. 그룹 헤더의 콘텐츠 offset(가상화 실측 `getOffsetForIndex`)으로 월 마커 생성, 그룹 키 길이로 줌 판별(연 "2025"/월 "2025-12"/일 "2025-12-30")해 월 단위 중복 제거 → 연 뷰=연도 라벨, 월·일 뷰=연도+월 라벨, 드래그/클릭 점프. 기존 고아 `Scrubber.tsx` 재사용. ② **장소 지역별 뷰**(`PlacesRegionView`): 평면 칩 목록 → 국가→지역 2단 폴더형. 국가 카드(🌐+개수) → 지역 카드(폴더형+직속 사진 4장 미리보기) → 사진 썸네일 그리드(readonly 라이트박스), 브레드크럼 상위 이동. 지오코딩 그룹 이름을 첫 토큰=국가/나머지=지역으로 파싱(방어적, 실 NAS name 포맷 미검증), 기존 `places()`/`place_items()` 재사용(백엔드·좌표 변경 0). **검증**: mock UI(스크러버 3뷰 라벨·점프, 장소 국가→지역→사진→라이트박스), 빌드 통과. **후속(사용자 확인 대기)**: 사람 탭 썸네일 — mock 정상이라 실 NAS Person API 응답 형태 문제로 판단(사용자가 네트워크 응답 확인 후 수정); 장소 **지도 뷰**(GPS 좌표 백엔드 프로브 + 지도 라이브러리/OSM 타일 선행 — 지역별 뷰 우선 결정).
- [x] **앨범/뷰어 성능 개선 (2026-07-06, 사용자 "느려" 보고)** — ① **미리보기 카드**(장소 도시·폴더)가 4장 보여주려고 그룹 전체 목록(수천 장+썸네일 메타)을 통째로 페이징하던 문제 → `place-items`/`folder-items`에 `limit`(1~100) 추가, 미리보기는 한 페이지만 단일 요청(`_filtered_items` limit이면 페이징 중단), 미리보기 쿼리 키를 전체와 분리(place-preview/folder-preview). ② **비디오 탭**이 라이브러리 전량을 매번 순차 스캔(type==video 앱단 필터)해 느리던 문제 → 결과를 `_VIDEO_CACHE`에 buckets와 같은 창(5분) 캐시(쓰기 시 `invalidate_bucket_cache`가 함께 비움) + 전량 스캔을 병렬 "물결"로(공용 `_scan_item_pages`, `_SCAN_CONCURRENCY=6`, total 미신뢰→짧은 페이지에서 종료). ③ **타임라인 buckets()도** 같은 헬퍼로 병렬화(사진 뷰어 첫 로딩 단축). **검증**: pytest 133(limit 단일페이지/캡, 비디오 필터·캐시·무효화·병렬 물결 신규), mock UI(미리보기 요청 limit=8·도시 진입 전체 병합). **남은 옵션**: 백엔드 썸네일 디스크 캐시(브라우저 캐시 미스 시 DSM 왕복 제거), DSM 네이티브 영상 필터 프로브.
- [x] **앨범/태그뷰 IA 재편 1단계 — 감상 렌즈 편입 + 앨범 영역 분리 (2026-07-07, 사용자 요청)** — 기존 "앨범" 영역이 실제로는 사람·장소·비디오 AI 자동 그룹(순수 뷰어)만 담아, 정작 사용자가 기대하는 "내가 만드는 앨범"이 없고 개념이 뒤섞이던 문제. **결정(A안 확장, 최상위 3영역 유지)**: 사람·장소·비디오는 AI 자동 그룹이라 "앨범"이 아닌 **감상 렌즈**로 보고 **사진 뷰어로 편입**, "앨범"은 **사용자 큐레이션 전용**으로 비움. **구조**: 스토어 `albumKind`(people/places/videos) 제거 → `viewerLens`(timeline/folder/people/places/videos) 도입, `ViewerZoom`에서 folder 제거(폴더는 렌즈로 승격). `ViewerScreen`에 렌즈 바 [타임라인·폴더·사람·장소·비디오] + (타임라인) 연/월/일 줌 + (사람) 그룹 브레드크럼. AlbumsScreen→`TagLenses.tsx`(사람/장소/비디오 본문, 탭 바는 ViewerScreen 소유)로 이동, 새 `AlbumsScreen`은 사용자 앨범 플레이스홀더(＋앨범 만들기는 2단계까지 비활성). **뒤로가기**: `selectBackDepth`의 줌 항을 `viewerLens==="timeline"`으로 게이팅, navHistory 스냅샷을 **평탄화**(zoom=year·group=null)해 항목당 정확히 뒤로 1회(드릴 후 렌즈 전환 시 깊이 숨김→popstate 중 skippable 센티넬 재발 방지). 사람 그룹 드릴은 기존 `groupId`(뒤로가기 통합) 유지. **검증**: mock UI(렌즈 5종 전환, 사람→그룹 드릴→뒤로 2단계로 목록·타임라인 복귀, 앨범 플레이스홀더, 폴더 렌즈 마운트), tsc/빌드 통과. 백엔드·pytest 무변경(persons/places/videos 엔드포인트 그대로). **2단계(실 NAS 필요)**: DSM Photo Album API 프로브(`SYNO.Foto.Browse.NormalAlbum` 등) → 백엔드 앨범 소스/엔드포인트 → 실제 생성/추가 UI. (147항 후속 "앨범생성"의 IA 확정분.)
- [x] **앨범 생성/관리 2단계 — DSM 앨범 CRUD (2026-07-07, 사용자 요청)** — **실 NAS 프로브(2026-07-07)**: 앨범은 **개인 공간 전용** — `SYNO.Foto.Browse.Album`(v1-5 목록)·`NormalAlbum`(v1-4 생성/추가/삭제)·`ConditionAlbum`(스마트) 존재, **FotoTeam엔 앨범 API 없음**(공유 앨범 미지원). **백엔드**: `PhotoSource`에 albums/album_items/create_album/add_to_album/delete_album 추가 — `DsmPhotoSource`는 `SYNO.Foto.Browse.Album` list + `Browse.Item` album_id 필터(read) + `NormalAlbum` create/add_item/delete(write), `MockPhotoSource`는 인메모리 앨범 스토어(데모 앨범 1개 시드), API 5종(GET albums·album-items, POST albums·albums/add, DELETE albums/{id}). 앨범은 라이브러리 셀렉터와 무관하게 항상 personal(사람 렌즈처럼). **프론트**: `AlbumsScreen`을 실제 목록(커버 카드+개수)·생성(이름 프롬프트)·열람(readonly 그리드)·삭제로, 앨범 열람은 screenBackDepth 보고(뒤로가기 통합). **담기**: 폴더 분류 선택 액션바에 "앨범에 추가" → `AlbumPickerDialog`(기존 선택/새 앨범 생성), 1차 구역·타인 라이브러리에선 숨김(앨범=개인). **검증**: pytest 143(앨범 CRUD 생명주기·로그인 게이트 2건 신규), mock UI e2e(목록·데모앨범 열람·폴더 8장 선택→담기→개수 8→16), tsc/빌드 통과. **남은 것(실 NAS 검증 대기)**: `NormalAlbum` create/add_item/delete의 정확한 메서드·파라미터·응답 형태(커뮤니티 문서 기반, name_person 병합과 동일한 best-effort). 팀 사진→개인 앨범 담기 가부(개인 전용이라 실 NAS에선 personal 선택 권장).
- [x] **분할 뷰 루트 대상 이동/복사 (2026-07-07, 사용자 보고)** — 1차 구역→내 사진처럼 영역 간 폴더/사진을 옮길 때, 대상 페인이 **루트(최상위 폴더 목록)** 면 `destCurrent==null`이라 가운데 이동/복사 버튼이 활성화되지 않던 문제(대상 폴더를 열어야만 켜짐). "1차 백업→2차 최상위로 이동"이 정작 막혀 있었다. **해결**: `_dest_dir`가 `"root:<space>"` sentinel을 인식해 `_share_prefix(space)`(개인=/home/Photos·공용=/photo)로 해석 → move/move_folders/conflicts/folder_conflicts가 **시그니처 변경 없이** 루트 대상 지원(스키마 무변경). 프론트 `DualActions`에 대상 페인 영역(`destArea`) 전달, 대상이 루트면 `root:${destArea.space}`로 보내고 버튼 활성(단 1차 구역·타인 라이브러리 루트는 경로 해석 범위 밖이라 제외 — 개인/공용 루트만). 이동은 경로 기반 FileStation CopyMove라 zone 소스가 개인 Photos 경로로 계산됨(zone_source가 personal prefix 미하이재킹). mock은 합성 루트 폴더 + no-op 가드(자기 자신 충돌 방지). **검증**: pytest 145(사진·폴더 루트 이동 2건 신규), mock UI e2e(분할 뷰 대상 루트 상태에서 폴더 체크→이동 버튼 활성→이동 토스트+되돌리기), tsc/빌드 통과. **남은 것(실 NAS 검증)**: zone→개인 최상위 실 CopyMove 동작(경로 해석·충돌·undo).
- [x] **백엔드 썸네일 디스크 캐시 (2026-07-07, 사용자 "너무 느려" 보고)** — 썸네일은 (item·cache_key·size)당 동일 이미지라 매 요청마다 DSM으로 왕복하던 것을 로컬 디스크에 캐시해 반복 조회를 즉시 응답. **특히 1차 구역(FileStation) 썸네일**이 느린데, 실 NAS 조사 결과 `/homes/luckyguy/Personal` 백업 폴더의 리프에는 `@eaDir/<파일>/SYNOPHOTO_THUMB_{SM,M,XL}.jpg`가 **일부만** 생성돼 있어(미생성 파일은 DSM이 온디맨드 생성 → 느림), 캐시로 2회차부터 왕복을 제거한다. **구현**: `photos/thumb_cache.py`(`ThumbCache`, sha256 키·2-hex 샤딩·원자적 쓰기·TTL 30일·best-effort), `/api/photos/thumbnail`이 mock 외에서 캐시 조회→히트 즉시 반환/미스 페치 후 저장. **접근 제어 보존**: 키에 요청 계정 포함(다른 사용자가 id만으로 남의 개인/1차 썸네일을 캐시에서 못 받음). 캐시 위치 `/data/thumb-cache`(영속 볼륨). **검증**: pytest 149(캐시 put/get·계정 격리·TTL 만료·무음 실패 4건 신규), import/빌드 통과. **남은 것**: 능동적 eviction 없음(orphan은 TTL로만 만료·삭제는 안 됨) — 필요 시 `/data/thumb-cache` 삭제로 초기화, 향후 크기 상한+LRU 정리 옵션. 1차 구역 첫 로딩 속도는 DSM 인덱싱 문제(폴더 썸네일 미생성분)라 앱 밖 — 사용자가 해당 폴더를 DSM에서 인덱싱하면 개선.
- [x] **1차 구역 썸네일 화질·동영상 미표시 (2026-07-07, 사용자 보고)** — 실 NAS 조사로 근본 원인 확정: 파일 옆 `@eaDir/<name>/`에는 사진·동영상 **모두** 미디어 인덱스 썸네일 `SYNOPHOTO_THUMB_{SM,M,XL}.jpg`가 있는데, 앱이 쓰던 `SYNO.FileStation.Thumb`는 **소형 `SYNOFILE_THUMB`만 서빙**한다. 사진엔 SYNOFILE이 있어 저해상으로 뜨고(일부 폴더는 SYNOFILE만 있어 더 나쁨), **동영상엔 SYNOFILE이 없어 404 → ▶ 폴백(썸네일 미표시)**. **1차 시도(폐기)**: `thumbnail()`을 `@eaDir/.../SYNOPHOTO_THUMB` 직접 다운로드(`SYNO.FileStation.Download`)로 전환 → **DSM API가 @eaDir을 차단**(실 NAS 로그: 사진은 폴백 SYNOFILE로 200, 동영상은 404 지속 + 매 요청 2회 호출로 지연). 폐기. **최종 해결(마운트)**: `/volume1/homes`를 컨테이너에 **읽기전용 마운트**(`/photovol/homes`, env `THUMB_MOUNT_HOMES`)하고, homes/1차 path-id 썸네일은 `@eaDir/<name>/SYNOPHOTO_THUMB_{M|XL}`을 **디스크에서 직접 읽어**(`asyncio.to_thread` 논블로킹) 서빙. API 차단은 앱 계층 정책이라 FS 직접 읽기엔 안 걸림 — 실측(appuser uid 10001, 읽기전용 마운트로 404났던 동영상 포스터 43KB 정상 읽음). 사진 고화질(M=320px)+**동영상 포스터** 해결, DSM 왕복 없어 더 빠름. 미설정/SYNOPHOTO 부재 시 `FileStation.Thumb` API 폴백(무회귀). 캐시 버전 `v3`. **아키텍처 예외 명시**: "파일 조작은 DSM API로만" 원칙을 **읽기 전용 썸네일에 한해 완화**(쓰기/이동/삭제는 계속 DSM API 권한 enforce). prod compose에 마운트+env, 헬스체크 timeout 5s→15s·start_period 40s(오탐 완화). **검증**: pytest 149, import/빌드, 실 NAS 마운트 읽기 실측. **남은 것**: 실기기에서 동영상 포스터·사진 화질 최종 확인.
- [x] **폴더 이름 정리 — 날짜부 밑줄→하이픈 (2026-07-07, 사용자 요청)** — 이벤트 폴더 기본은 `YYYY-MM-DD 이벤트`인데 일부가 `YYYY_MM_DD`(밑줄). 열어놓은 폴더 하위를 재귀 스캔해 규칙 위반 폴더를 찾아 `현재→교정안` 검토 목록으로 보여주고 체크한 것만 이름 변경. **규칙**(`folder_naming.fix_date_prefix`): 맨 앞 `YYYY_MM_DD`의 밑줄만 하이픈으로, 이벤트명 밑줄은 유지. **백엔드**: `audit_folder_names`(마운트 os.walk로 재귀 스캔 — 소유자 uid 컨텍스트라 빠름·DSM 불필요) + `rename_folder`(FileStation.Rename, 제자리 — 사진·썸네일·@eaDir 유지). 1차 구역/homes(경로형 id) 전용, Foto는 미지원(빈 목록/명확한 오류). API 2종(GET folder-name-audit, POST folders/rename). **프론트**: FolderPane 브레드크럼 "이름 정리" 버튼(1차 구역/타인 컨텍스트에서만) → `FolderNameAuditDialog`(스캔→체크박스 목록→순차 적용). 이름 변경은 undo 없음이라 검토-선택 적용이 안전장치. **검증**: pytest 152(규칙 유틸·audit/rename 생명주기·게이트 3건 신규), tsc/빌드 통과. **남은 것**: 실 NAS에서 FileStation.Rename 동작 확인.
- [x] **촬영일 교정 — 파일에 EXIF+mtime 실제 기록 (2026-07-09, 사용자 요청)** — 1차 구역·내 사진 사진의 촬영일이 파일 mtime(복사시각 2026-07)으로 오염됨(EXIF 촬영일 없는 카톡/앱 사진 → 앱·Synology 모두 mtime 폴백). 실측 23,942개 중 ~90%가 파일명에 진짜 촬영일 보유. **목표: 1차 구역에서 정리 후 내사진/공용 이동 시에도 촬영일 유지** → "표시만 교정"은 이동 시 소실되어 부적합, **파일 자체에 새김**. 구현: `capture_date.py`(파일명 파서 ms/sec epoch·YYYYMMDD[_-]HHMMSS·YYYY-MM-DD·Screenshot_*·IMG_* + EXIF 읽기 + resolve[EXIF 우선]) + `capture_fix.py`(JPEG=EXIF DateTimeOriginal via piexif + 전 포맷 mtime; EXIF 촬영일 있으면 미변경). API: `capture-audit`(드라이런)·`capture-fix`(자동)·`capture-fix-manual`(수동). 프론트 `CaptureDateDialog` + FolderPane '촬영일 교정' 버튼(본인 홈 매핑 폴더만). **⚠️ 아키텍처 예외**: homes 마운트 `:ro→:rw`(DSM API엔 EXIF/mtime 설정 없음) — 대상은 소유자(uid 1026) 본인 파일만, 그 외 파일작업은 계속 DSM API. **되돌리기 없음**(EXIF 덮어씀) → 드라이런 검토가 안전장치. 공용(team, /photo root 소유)은 파일 쓰기 불가 → Phase 2(Synology 촬영시간 API). **검증(2026-07-09)**: 임시 JPEG EXIF 기록·audit(2025-12 폴더 85장 전원 자동감지) 확인. **미확인**: 내 사진 파일 교정 후 Synology 자동 재인덱싱 여부(첫 실적용 시 확인).
- [ ] PhotoPrism식 Stacking(비파괴 대안) — 옵션 검토 항목으로 유지(미구현)

## F. 전체 리뷰(2026-07-09, 3에이전트 분석) — 속도 개선 로드맵
- [x] **Phase A(배포·실측 완료)** — 타임라인 3계층 캐시(L1 scope 공유/L2 SQLite/SWR 백그라운드 재스캔), 트래시 병렬·증분 캐시(전역), gzip(썸네일·비디오 제외), 썸네일 immutable 1y+ETag/304+SW CacheFirst, staleTime 5m, `_FOLDER_META` rename/move 정확성 수정. **실측: 공용(69k장+트래시 158폴더) 타임라인 콜드 300s 타임아웃→첫 스캔 17.6s(1회)→웜 0.0s·재시작 후 0.04s.**
  - ⚠️ **코드 스플리팅(Albums/Manage/PlacesMapView lazy)은 되돌림(ec0c352)** — 기존 설치 PWA의 옛 서비스워커가 새 동적 청크를 못 받아 Suspense 무한 대기("불러오는 중"에서 앱 전체 멈춤). 진단: 신규 브라우저는 정상 렌더 → 빌드/백엔드 정상, 기기 SW 캐시 문제. 단일 번들(578KB/gzip 175KB)이 안정. 재도입하려면 lazy 실패 시 1회 자동 새로고침 래퍼(new SW로 치유) 필수. [[nas-photo-pwa-cache]]
- [x] **긴급 수정(a941dd5, 2026-07-09 밤)**: 세마포어 기아 장애 해소 — 재시작 직후 트래시 재구축이 요청마다 중복·무제한 병렬 발사되어 DSM 세마포어(24) 포화 → 로그인·folders·items 전부 기아(로그인 무한대기+사진 안 뜸). 수정: 트래시 재구축 single-flight(Lock)+동시성 6 상한, 인증은 세마포어 우회, 백그라운드 재스캔 태스크 강한 참조(GC 방지), 로그인 시도 선기록+인증 타임아웃 12s(d5b798c). 검증: 재시작 직후 동시부하(items×5+folders+login) 전부 완료, folders 60s타임아웃→1.8s, 웜 0.1s.
- [~] **Phase B(체감)** — 2026-07-10 추가 완료(3ed775b~01c9b1f): 그리드 3종 행 가상화(VirtualRows — FolderPane·SearchView·FolderViewerGrid, 실측: 909장 폴더 DOM 62img/888건 검색 DOM 50img만 마운트), folder-counts 청크(20개) 키, 썸네일 unit_id 수정(복사본 404 — cache_key 접두가 unit), 검색 키워드 json.dumps(숫자/날짜꼴 code 120; 한국어는 escape·평문 모두 동작 확인), VirtualRows 함정 2건(부모 scroll ref 늦은 attach→scrollEl state 승격, RO 초기 전달 미보장→초기 폭 동기 측정). '고희연' 검색 0건은 DSM 특성(팀 폴더명 미매칭·파일명/태그만).
- [~] **Phase B(이전분)** — 완료(264802b): CopyMove 청크 25→바이트예산 패킹(200개/6KB — URL 414 함정 회피, 500장 이동 20→3~5태스크), 적응형 폴링(0.1s→0.5s), undo 목적지별 그룹화(500태스크→폴더수), 라이브러리 전환 clear()→스코프 제거(세션·메타 유지). (이후 전부 완료: 가상화 3종 3ed775b, counts 청크 키, 낙관적 업데이트+resettle 2회 fa83834)
- [~] **Phase C(구조)** — 완료(f336eee·100675f, 2026-07-10): capture-audit 재귀 병렬화(subtree 레벨 BFS·수집 유계 6) + EXIF 판독 스레드풀 8 — **/2008 재귀 audit 146s→27.4s(실측)**; persons/places (sid,space) TTL 캐시(6.0s→0.0s)+name_person 재스캔 흡수·변경 시 무효화; get_session 30s 프로세스 캐시(요청당 SQLite 제거, 삭제 시 무효화); 세션 만료/로그아웃 시 sid 캐시 회수(drop_session_caches — 메모리 누수 해소); _ensure_dir 오류 로그화; 죽은 코드 제거(DateHeader.tsx·rowModel buildRows/placeholderRows/미사용 타입·@air/react-drag-to-select). **남음**: dsm_source.py 분할(browse/fileops/cache — 2100줄, 별도 세션 권장). (prompt→공용 Modal은 8043d32로 완료)
- [x] **한국 모바일 사진 표시 경로 전면 개선(2026-09-02)** — ① 월뷰의 미로드 월을 1행으로 잡아 overscan에 여러 달이 들어오고, 보이는 그룹의 모든 날짜 API를 한꺼번에 발사하던 구조를 **날짜별 count placeholder + 가시 날짜만 점진 요청**으로 교체(스크롤 높이도 응답 전후 안정). `scrollToGroup`도 한 달 전량 대신 첫 날짜만 선요청. ② 폴더·지역 카드 미리보기 N+1을 `IntersectionObserver`(스크롤 루트 + 500px margin)로 근접 카드만 조회. ③ 라이트박스는 현재 XL 디코딩 전 이웃 XL 2개를 경쟁시키지 않고, 완료 후 다음→이전 순차 예열. ④ ThumbCache 읽기는 `to_thread`, 쓰기는 응답 후 BackgroundTask로 옮겨 FastAPI 이벤트 루프·응답 꼬리 지연 제거. 5천 장 초과 단일 날짜의 DSM 후속 페이지는 유계 4병렬. ⑤ 브라우저/SW 썸네일 캐시는 서버 발급 **세션별 opaque scope**가 URL 첫 키가 되고 서버가 scope를 세션에 검증; 로그인 전환·로그아웃·401에서 CacheStorage와 React Query를 정리해 공용 기기 계정 간 노출 차단. 구형 무스코프 캐시는 1회 제거, 새 SW는 scope 없는 응답을 저장하지 않음. ⑥ 운영 쿠키 기본 `Secure=true`, IP 로그인 제한 추가. 네트워크는 `photo.specialrisk.me`만 Cloudflare DNS-only로 전환해 한국→해외 CF PoP→집 우회를 제거하고, 가족사진 응답은 계속 `private`(공용 엣지 캐시 금지).
- [x] **추가 병목 검증·보강(2026-09-02)** — 모든 async 목록 API의 동기 `fill_thumbhashes` SQLite 조회를 `asyncio.to_thread`로 옮겨 동시 사용자 시 이벤트 루프 정지를 방지하고, 가상화된 그리드 `<img>`에 `decoding="async"`를 명시했다. 운영 DB 실측은 47개 p50 0.449ms·500개 p50 1.883ms여서 이 SQLite 호출은 0.2초 응답의 지배 원인은 아니며 방어적 개선이다. 일자 배치 API는 DNS-only+HTTP/2 전환과 현재 가시 날짜 단위 가상화 뒤 기대 이득이 작고 캐시·부분실패 복잡도가 커 보류했다. `loading="lazy"`도 Chromium의 느린 연결일수록 사전 로드 거리가 늘고 가상화 overscan보다 넓어 전역 eager 전환 없이 유지했다.
- [x] **Gallery→Desk 전환 안전장치 G-A(2026-09-02)** — `GALLERY_WRITE_MODE=legacy|drain|curation`과 관리자 전용 레거시 Synology 날짜 교정 스위치를 추가하고, 숨긴 UI를 우회해도 신규 파일·폴더/중복/정리 mutation이 `drain`·`curation`에서 403이 되도록 중앙 FastAPI dependency를 모든 경계에 적용했다. `drain`은 기존 undo·개별 휴지통 복원만 유지하고 영구 비우기를 차단하며, `curation`은 복구도 닫는다. 앨범·인물 이름/병합은 논리 큐레이션으로 계속 허용한다. `/api/system/info`가 effective capability를 반환하고 프론트의 액션바·DnD·폴더 도구·촬영일·구역 관리·작업복구 UI가 이를 fail-closed로 따른다. 첫 prod compose는 `legacy`로 고정해 운영 동작을 바꾸지 않는다. 검증: 모드/역할/라우트 표 테스트 17건 포함 pytest 221, TypeScript+Vite production build 통과. 상세 인계는 `docs/DESK_G0_HANDOFF.md`.
- [~] **Phase D(기능 균형)** — 완료: 앨범 이름변경·사진 빼기(620917a), 휴지통 개별 복원(fa83834), 박스선택 미구현 확정·검색창 숨김·B-7(87d6ec2), 명세 §9 현행 IA 블록. **남음**: 감상 화면에서 앨범 담기(뷰어 선택 모델 필요 — 대형), 명세 §9 원문 전체 재작성(현행 IA 블록만 삽입됨), IMPROVEMENTS 문서 대청소
- [x] CLIP 임베딩 기반 탐지(Immich 방식)는 NAS 사양·목적상 **비권고** — 도입하지 않음(설계 준수)

---

## E. DSM 연동 참고 (구현 시 상시 참조)

- 조회·메타데이터·썸네일 = `SYNO.Foto.*`(개인) / `SYNO.FotoTeam.*`(공유 — **네임스페이스 분기가 최대 함정**), 삭제·이동 = `SYNO.FileStation.*` 역할 분담. Photos item ↔ 실제 파일 경로 매핑 관리 필요
- 2FA: `otp_code` + **device_name/device_id 저장으로 "신뢰 장치" 등록 → 이후 OTP 생략** 흐름 지원 검토 ([N4S4/synology-api](https://github.com/N4S4/synology-api) 참고 — 단 requests 기반 sync라 직접 의존은 비권고, 현 async 클라이언트 유지)
- 비공식 Photos API 문서: [zeichensatz/SynologyPhotosAPI](https://github.com/zeichensatz/SynologyPhotosAPI)
- API 버전은 런타임 `SYNO.API.Info` 조회(현 구조 유지 — 하드코딩 금지)

---

## 반영 현황

- [x] 명세서(`NAS_사진정리앱_개발명세서.md`) 6·9·11·13장에 위 결정 반영 (2026-07-02)
- [x] README에 이 문서 링크 추가
- [x] 프로젝트 `CLAUDE.md`에 강제 참조 규칙 명시
- [x] A절 백엔드 수정 구현 (2026-07-02) — A-1~A-5 완료, 단위 테스트 26개 통과
- [x] B·C절 기반 **타임라인 마일스톤** 구현 (2026-07-02) — count-first 버킷 API + MOCK_MODE(NAS 불필요), justified 행 가상화, 스크러버, 선택 3종(체크서클/Shift 프리뷰/박스), DnD 셸(DragOverlay·폴더 드롭), 미니 라이트박스, 토스트
- [x] **파일 작업 + 작업로그/Undo 마일스톤** 구현 (2026-07-02) —
  - 백엔드: PhotoSource 변이 프로토콜(move/copy/delete/undo 프리미티브), mock 상태 오버레이(cross-space 이동·복사·휴지통·복원이 실제로 동작), `operations.py` 작업로그 서비스(역연산 payload, 7일 undo 기한), `/api/photos/ops/*`·`/api/ops` 라우터, target_user 감사 로깅(관리자만 허용)
  - 프론트: `useFileOps` 훅(선택 자동 해제 + 정밀 무효화 + **되돌리기 액션 토스트**), 폴더 드롭→실제 이동(⌥=복사), 액션바+폴더 피커(공용 보내기는 복사 기본 — spec ch.4), **작업 기록 패널**(항목별 되돌리기), 라이트박스 완성(`i` EXIF 패널·Delete 자동 전진·Shift+? 도움말), **폴더 뷰**(spec 9.3), 관리자 셸(멤버 선택 + 주황 배너 + 감사 로깅)
  - 검증: pytest 56개 통과, tsc+vite 빌드 통과, mock 서버 e2e 스모크(이동→폴더→삭제→undo→복원), **Docker 이미지 빌드+컨테이너 기동 검증**(헬스체크·정적 프론트·non-root)
- [x] **D절 기반 2단계(중복 제거) 구현** (2026-07-02) — 자체 pHash(Pillow만)+SHA-256, photo_cache 해시 영속화, SQLite 잡+asyncio 워커(진행률/취소/재개), 8밴드 버킷팅+union-find 그룹핑, 중복 정리 뷰(스캔 진행바·임계값 슬라이더·reference 뒤집기·dry-run·기존 삭제/undo 재사용). v1 한계 4건 해소(스크롤 앵커링, 라이트박스 이동, spring-loaded, autoScroll 확인). 테스트 69개 통과
- [~] DSM 실연동 검증 (실 NAS 192.168.0.10, DSM 7.2 — 진행 중)
  - [x] 조회: 로그인·역할판별(is_manager)·folders·Timeline(v1 고정)·KST buckets(62636장)·items·thumbnail — 전부 정확 동작
  - [x] **파일 작업(공용 공간): 이동/복사/삭제/폴더생성 + 각 undo 전부 실 NAS 검증** (iPad 폴더에서 실행 후 원상복구). 발견·해결: photo 휴지통 비활성→앱 관리 휴지통(`/photo/#trash/t<ns>/`)으로 삭제/복원, `#`폴더는 계층별 생성, 순수숫자 폴더명 금지(`t` 접두), item→경로는 Browse.Item get+folder
  - [x] **개인 공간(SYNO.Foto) 검증** — 프리픽스 `/home/Photos`(=`/homes/<user>/Photos`) 확정. buckets/items(29047장) count 정확, `/home/Photos`에서 CopyMove/CreateFolder/Delete 이동→복구 실동작(s.png). **발견·수정한 버그**: 하루 2148장(모바일 백업) 날짜에서 `items()`가 limit 1000에 잘림 → 페이지네이션으로 전량 반환
  - [x] **cross-space 이동(개인↔공용)** — `/photo` ↔ `/home/Photos` 양방향 CopyMove 실검증(에러 없음, 원상복구). 앱 `move`는 src/dest space별 프리픽스 계산으로 지원
  - [x] **폴더 트리 lazy 구현** — 실 NAS 폴더가 **1500+**, 전량 재귀는 40초라 비현실적. `SYNO.Foto.Browse.Folder`는 계층적(`id`로 직속 하위)이므로 **한 레벨씩 lazy 로드**(`folders(parent_id)`)로 재구현. 최상위(느림 11초)는 캐시+병렬, 하위는 즉시(0.03초). id→(space,path) 메타캐시로 파일작업 경로 해석. 프론트 `FolderTree` 재귀 컴포넌트(펼칠 때 하위 fetch, 드롭 대상). 실 NAS 이동→undo 통합 검증
  - **동작 노트**: 파일 이동/undo 후 Photos 재인덱싱으로 item id가 바뀜(예 1692→92388). 실사용은 작업마다 프론트가 buckets/items/folder-items 무효화·재조회하므로 새 id를 받아 정상. stale id 재사용만 주의
  - [x] **2단계 dedup 실스캔** — 개인 공간 29047장 스캔(28529 해시, ~518 broken 썸네일 skip, ~9분 @ 동시8·청크100). 결과 **8214 그룹, 24GB 절약가능**(exact 정확중복 + similar 유사). 연속촬영/버스트가 pHash로 정확히 묶임 — 자체 pHash 실데이터 검증 완료. **개선**: 스캔 병렬화(동시8)+청크(100)단위 진행률·저장, 개별 썸네일 실패는 skip(전체 실패 방지)
  - [~] **관리자 타인 폴더 — 데이터 소스 구현 완료(2026-07-03)**, 실데이터 검증만 대기. 사용자 보고("동작 안 함")로 전체 점검 → 원인 2중: ① 읽기 경로가 viewedOwner 미전달(UI 셸만) ② 타인 홈에 Photos 폴더 자체 부재(실 NAS 프로브: <user>만 존재, 나머지 408). **구현**: `HomesPhotoSource(DsmPhotoSource)` — 개인 공간만 FileStation(`/homes/<user>/Photos`)으로 재매핑(List 브라우즈 + Thumb 썸네일(raw 200 검증) + 경로형 id), `_item_meta`/`_dest_dir` 오버라이드만으로 이동/삭제/undo 파이프라인 상속. `get_photo_source` 의존성이 `?target_user=` 쿼리로 라우팅(관리자 검사는 mock 분기보다 앞 — 모드 무관 규칙, pytest 3건). 프론트는 client 중앙에서 viewedOwner 자동 전파 + 멤버 전환 시 캐시 clear + 폴더 뷰 자동 전환. **타인 공간은 폴더 보기 전용**(타임라인/인물/검색은 Photos 인덱스가 본인 세션 전용이라 빈 응답 + 배너 안내). members()는 Photos 폴더 있는 계정만 노출(tmbackup 등 시스템 계정 제외). **남은 것: 타인 계정에 사진 넣고 실 NAS 검증**(사용자가 추후 진행)
  - [x] **공용 공간 dedup 스캔** — 62626장 해시(~25분), **10582 그룹, 66GB 절약가능**. 대용량 영상의 정확 중복(원본명+날짜명으로 이중 저장된 mp4/MOV)이 다수 검출 — SHA-256 정확중복이 실데이터에서 큰 효과. 두 공간(공용+개인) 해시가 photo_cache에 영속(브라우저 중복정리 탭에서 즉시 조회)
  - [x] **폴더 뷰 우측 메인 드릴인(Finder식)** (2026-07-02) — 좌측 트리뿐 아니라 우측 메인창에서도 하위 폴더 카드로 탐색. 브레드크럼 + `FolderCard`(클릭=드릴인, 드롭 대상) + 직속 사진. 좌측 트리 선택 시 경로 리셋
  - **동작 노트(사진 표시 범위)**: `folder_items`(SYNO.Foto.Browse.Item, folder_id 필터)는 **해당 폴더 직속 사진만** 반환(하위 폴더 사진 미포함, 실 NAS 확인). 따라서 하위 폴더만 있는 **중간 폴더는 사진 0** — 로딩 문제 아님. 리프 폴더로 드릴인하면 사진이 보임. UX: 중간 폴더에서 "직접 담긴 사진 없음, 하위 폴더를 열어 보세요" 안내 추가
  - [x] **`folder_items` 페이지네이션 버그 수정** (2026-07-02) — 리프 폴더가 1000장을 넘으면 limit 1000에 잘리던 문제를 `items()`와 동일하게 전체 페이지 순회로 수정
  - [x] **폴더 뷰 리스트/아이콘 전환 + 사진 수 배지 + 패널 리사이즈** (2026-07-02) — ① 하위 폴더 섹션에 `▦ 아이콘/☰ 리스트` 토글(localStorage 유지, 두 페인 공통). ② 폴더별 직속 사진 수 배지: 백엔드 `folder_count`(DSM `SYNO.Foto.Browse.Item`의 `count` 메서드 — 전체 페이징 없이 1콜) + `GET /api/photos/folder-counts?ids=`(레벨당 1회 배치, 병렬, 실패 id는 생략해 배지만 숨김; **실 NAS에서 count 메서드 응답 확인 필요**). **→ 2026-08-13 하위 전체(재귀) 합산으로 변경**: 직속만 세면 자식이 폴더뿐인 중간 폴더(연도 폴더 등) 배지가 0장 — 모든 소스에서 서브트리 합산으로 수정(Foto는 마운트 THUMB_MOUNT_TEAM/HOMES 디스크 재귀 카운트 우선·없으면 서브트리 count 상한 병렬 합산, homes/zone은 `_count_media_on_disk` os.walk 재귀, mock 2종 동조). 회귀 테스트 `test_folder_counts.py` + API/zone 테스트. ③ 좌측 패널(폴더 뷰 트리 + 타임라인 패널) 드래그 리사이즈 — `useResizableWidth` 훅(pointer capture, 160–480px, localStorage 유지)
  - [x] **타임라인 빈 화면 버그 수정** (2026-07-02) — `TimelineView`의 ResizeObserver 부착 effect가 `[]` 의존성이라, buckets 로딩 스피너 조기 반환 중에 한 번 실행되고 끝나 그리드 마운트 후에도 width=0 → 행이 전혀 안 그려지는 잠복 버그(마운트·쿼리 타이밍에 따라 간헐 발생). `scrollEl` 상태(콜백 ref)를 deps로 걸어 그리드 실마운트 시 재부착으로 수정
  - [x] **타임라인 좌측 폴더 패널에 내비게이션 역할 부여** (2026-07-02) — 기존엔 드롭 대상 역할만 있어 클릭해도 무반응(무의미해 보임). 폴더 클릭 시 **폴더 보기로 전환 + 해당 폴더 열기**(store `pendingFolderPath`/`openFolderView` → FolderView가 소비해 pane A에 적용). 안내 문구도 두 역할(클릭=열기, 드래그=이동) 명시로 갱신. mock e2e 확인
  - [x] **폴더 뷰 분할(듀얼 페인) 정리 모드** (2026-07-02) — commander 패턴: 폴더 뷰에 `▤ 단일/▥ 분할` 토글. 분할 시 두 `FolderPane`이 독립 탐색(각자 브레드크럼·드릴인), 클릭한 페인이 활성(전역 선택 소유·파란 테두리), 가운데 액션바(**이동 →/←·복사·삭제**, 방향은 활성 페인 기준)로 반대쪽 폴더에 즉시 실행+Undo 토스트. DnD도 페인 간 동작(비활성 페인 배경+폴더 카드가 드롭 대상, droppable `data.folderId`로 페인별 id 충돌 회피). 메인 페인을 `FolderPane.tsx`로 추출해 단일/분할 공용. **부수 수정**: `PhotoItem.space?` 아이템 단위 space 전파 — 폴더 뷰가 개인 폴더 사진을 범위=공용 상태에서 보여줄 때 썸네일/이동/삭제가 잘못된 네임스페이스를 쓰던 문제(PhotoCell·Lightbox·DragOverlay·useFileOps 추론). 모바일: 분할은 상하 스택(lg 미만). mock e2e 검증: 분할 토글→양쪽 탐색→선택→이동→삭제→undo 전부 통과
  - [x] **상단 메뉴 IA 재편 v2 — 라이브러리 통합 셀렉터** (2026-07-03, 사용자 요청·A안 채택) — 어색함의 근원 진단: 범위(공용/개인) 칩과 사용자 드롭다운이 사실 같은 질문("어떤 라이브러리를 보나")인데 별개 위젯 + 폴더 뷰에서 칩이 사라지며 레이아웃 이동. **`[📚 공용 사진 ▾]` 단일 셀렉터**로 통합: 공용/내 사진/(관리자) 구성원 사진을 한 축으로, 뷰 무관 상시 표시. 타인 선택 시 셀렉터가 주황으로 변해 배너와 함께 상태 표시, 폴더 뷰 트리는 선택 라이브러리 섹션 우선 정렬. 헤더 = [라이브러리] | [뷰 4종] | [검색] | [계정] 2축 구조 완성. **후속(같은 날, 사용자 피드백)**: 라이브러리 스코프 전면 적용 — 폴더 뷰 루트는 선택 라이브러리 폴더만(양쪽 혼합 제거), 좌측 트리는 선택 라이브러리만 펼치고 반대쪽은 접힌 섹션(펼치면 드롭 가능 — 개인→공용 드래그는 스펙 핵심이라 접근 유지), 분류/검색 헤더에 라이브러리 배지(분류가 공용/개인 별도임을 명시)
  - [x] **상단 메뉴 IA 재편** (2026-07-02) — 성격이 다른 두 축(범위/보기)이 동일 알약 그룹으로 나란해 5개 평면 메뉴처럼 보이던 문제 해결. **보기**(📅타임라인/📁폴더/🔁중복정리)를 아이콘 주 메뉴로, **범위**(공용/개인)는 `범위` 라벨+세그먼트로 구분선 뒤 분리. `공용 폴더/내 개인 폴더`→`공용/개인`으로 개명해 '폴더' 단어 3중 충돌 제거. 폴더 보기는 공용·개인 트리를 동시에 노출하므로 범위 칩을 숨김(문맥 일치)
- [~] 3단계(AI 자동 분류) — **1차 구현 완료(2026-07-02)**. 방향: ① 기본 = Synology Photos 내장 AI 재활용, ② 옵트인 = 클라우드 비전 API(대표 사진만; 미구현), 로컬 ONNX 비권장.
  - [x] **실 NAS API 프로브** — `SYNO.API.Info`(무인증)로 확인: (Foto|FotoTeam).Browse.Person v1~3, Browse.Geocoding v1, GeneralTag/Concept/Search 전부 존재
  - [x] **백엔드**: protocol `persons/person_items/places/place_items` + dsm_source(Person list additional=thumbnail·show=false 숨김 필터·페이지네이션, Geocoding list, Browse.Item `person_id`/`geocoding_id` 필터 — `_filtered_items` 공용 헬퍼로 folder_items와 통합) + mock(결정적 인물 3·장소 2, 이름 없는 그룹 포함) + API 4종(`/api/photos/persons·person-items·places·place-items`)
  - [x] **프론트 ✨분류 뷰**: 인물(원형 커버+이름+n장)/장소(칩) 브라우즈 → 그룹 드릴인(사진 그리드, 전역 선택 모델 재사용 → 기존 액션바/DnD/라이트박스/진행바 그대로 동작) + "전체 선택" 버튼으로 폴더 모으기. 좌측 FolderPanel 동일 배치(드래그 이동 가능). 범위(공용/개인) 칩 적용
  - 검증: pytest 74(신규 2), mock e2e — 인물 3그룹/장소 2그룹 표시 → 엄마(451장) 드릴인 → 전체 선택 → '행사' 이동(진행 바 "70/451장 이동 중…" 표시) → undo 원복
  - [x] **실 NAS 데이터 검증(2026-07-02, 배포 후 사용자 확인)** — 분류 탭에서 인물·장소 그룹 정상 표시 확인(공용/개인)
  - [ ] (옵트인) 클라우드 비전 API 폴더명/그룹 제안 — 대표 사진만 전송, API 키 .env 옵트인
- 운영: 리버스 프록시는 **Nginx Proxy Manager 사용 확정**(2026-07-02) — DEPLOYMENT.md §3 절차 참조(Proxy Host → :9800, Let's Encrypt, 이후 `COOKIE_SECURE=true`)

### F-D 진행 메모(2026-07-11) — ①②③ 완료
- ①소형 UX(타인 열람 중 검색창 숨김·휴지통 비우기 차단 B-7·명세 §9 현행 IA·박스선택 미구현 확정) **완료**(87d6ec2).
- ②공용 다이얼로그: Modal+askConfirm/askPrompt(Esc·배경닫기·role=dialog·포커스), window.prompt/confirm 8곳 전부 교체 **완료**(8043d32).
- ③앨범 보강 **완료**(620917a·ae22bda): 이름변경(Browse.Album set_name v1 POST·json.dumps)+사진 빼기(NormalAlbum delete_item v1, 상세 '사진 빼기' 모드)+**기존 삭제 고장 수정**(NormalAlbum delete=103 → Browse.Album delete). 실 NAS 스모크: 생성→이름변경→삭제 전부 200. '감상 화면에서 담기'는 뷰어에 선택 모델이 없어 보류(대형 작업).
- ④(낙관적 업데이트·resettle 2회)·⑤(휴지통 개별 복원) fa83834로 완료. 타임라인 드릴 안착·크럼 동기화 33b5a7d 완료. 백업 앱 mtime 소스 보존(photo_manager 46a12d4, 2026-07-12) 완료 — 신규 유입은 촬영일 오염 없음.
- **현행 남음 목록(2026-07-12 정리)**: ⑴ 1차 구역 신규 유입 뱃지(서버+프론트 소형), ⑵ dsm_source.py 분할(별도 세션), ⑶ 감상에서 앨범 담기(대형), ⑷ 정리 마법사 D-139(대형·미결 4건 선행), ⑸ 문서 재작성(명세 §9·IMPROVEMENTS 대청소), ⑹ 선택: 업로드 전 해시 중복조회 API, 백업 완료 딥링크(photo_manager), 코드 스플리팅 재도입(lazy 실패 자동새로고침 래퍼 동반), 로그인 타임아웃 에러 문구 개선.

### (구) 앨범 보강 착수 메모
- ①소형 UX(검색창 숨김·비우기 차단·문서)·②공용 다이얼로그(87d6ec2·8043d32) **배포 완료**.
- ③앨범 보강용 DSM API **실 NAS 프로브 확정**: 이름변경=`SYNO.Foto.Browse.Album` `set_name` v1(POST, name은 json.dumps — NormalAlbum엔 set_name 없음/103), 사진 빼기=`SYNO.Foto.Browse.NormalAlbum` `delete_item` v1(id=<album>, item=json.dumps([ids])), 삭제=`Browse.Album delete` v1도 동작. 남은 구현: 백엔드 rename_album/remove_album_items + 라우트, 프론트 AlbumsScreen 이름변경 버튼·상세에서 선택→빼기, 감상 선택 액션바에 "앨범에 담기" 노출(canAddToAlbum 조건 재사용). 이후 ④무효화 스코프+낙관적, ⑤휴지통 복원 UI.

### 타임라인 연→월 드릴 이슈 — 해결 완료(33b5a7d, 2026-07-11)
- **완료**: 월뷰 상단 크럼을 실제 뷰포트 최상단 그룹으로 동기화(visibleGroupLabel + GroupedPhotoGrid onTopGroupChange) — 월 줌 버튼 진입 시 크럼=화면 일치 실기기 확인(6761f31).
- **근본 원인(목 환경 실측으로 규명)**: 가상화기 내부 scrollOffset이 프로그램 스크롤(scrollToIndex)을 따라가지 못하고 0에 고정 → getVirtualItems가 상단 범위만 렌더(깊은 위치는 빈 화면), 안착 판정 기준까지 오염. **해법**: 대상 offset을 getOffsetForIndex로 직접 계산해 el.scrollTo + 명시적 scroll 이벤트 dispatch로 동기화 강제, 판정·크럼 모두 el.scrollTop 기준. 검증: 목(2025-12)·실 NAS(2016-12) 모두 크럼=상단 헤더 정확 일치.
- (구 기록) 연뷰에서 사진 클릭 드릴 시 대상 월로 스크롤이 안 됐음. 이력: (a) 기존 1회 scrollToIndex — 위쪽 행 실측되며 밀림(원래 버그), (b) effect+onChange tick 기반 재안착 — 시도 상한/의존성 폭풍으로 불안정, (c) 현행 200ms 독립 타이머(GroupedPhotoGrid의 scrollToGroup effect) — 실기기에서 스크롤 미발생. **다음 진단**: 타이머 콜백에 console.log(scrollToGroup, idx, item?.start, virtualizer.scrollOffset) 임시 삽입해 (i) focusMonth 값 정상 여부(taken_at slice), (ii) rows에 대상 header 존재 여부(idx), (iii) scrollToIndex 후 offset 변화를 확인. scrollToIndex가 동작 안 하면 el.scrollTo({top: virtualizer.getOffsetForIndex(idx)[0]}) 직접 호출로 대체 검토.
