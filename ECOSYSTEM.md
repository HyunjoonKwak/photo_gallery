# 사진 앱 셋 — 역할과 종속성

> **이 문서는 세 저장소에 같은 내용으로 있습니다.**
> 고칠 때는 셋 다 고치세요. 정본은 [photo_desk/ECOSYSTEM.md](https://github.com/HyunjoonKwak/photo_desk/blob/main/ECOSYSTEM.md) 입니다.
>
> | 저장소 | 앱 이름 | 어디서 도나 |
> |---|---|---|
> | [photo_desk](https://github.com/HyunjoonKwak/photo_desk) | Photo Desk | 맥 (Tauri 2 · Rust · React) |
> | [photo_gallery](https://github.com/HyunjoonKwak/photo_gallery) | 우리집 사진관 | NAS (Docker · FastAPI · React) |
> | [photo_backup](https://github.com/HyunjoonKwak/photo_backup) | Photo Backup | 안드로이드 (Kotlin · Compose) |

---

## 1. 한 줄 역할

| 앱 | 하는 일 | **하지 않는 일** |
|---|---|---|
| **Photo Backup** | 폰 사진을 NAS 1차 구역으로 자동 백업 | 정리 · 분류 · 중복 판정 |
| **Photo Desk** | 맥과 NAS 1차 구역을 분류·정리해 공용·내사진으로 보냄. **모든 복잡한 조작** | 가족용 화면 · NAS 안에서의 직접 이동 |
| **우리집 사진관** | 가족이 공용·내사진을 감상 | 무거운 판정(중복·잡동사니·흐림) |

경계를 가르는 잣대는 **사진이 어디 있느냐가 아니라 누가 언제 만지느냐**입니다.
무거운 판단은 전량 사본이 있는 맥에서 합니다 — 52,570장을 2.5초에 훑습니다.

---

## 2. 사진이 흐르는 길

```
  폰 카메라
     │  ① Photo Backup 이 자동 백업
     ▼
  NAS 1차 구역  ←─ 가족·구글포토도 여기로 들어옴
     │  ② Photo Desk 가 rsync 로 내려받음 (nas_pull)
     ▼
  맥 «작업대» 라이브러리 / NAS-1차/
     │  ③ Photo Desk 가 분류·정리 (중복·잡동사니·흐림·인물·지명)
     ▼
  맥 «내사진» · «공용» 라이브러리
     │  ④ Synology Drive Client 가 동기화 (우리 코드 아님)
     ▼
  NAS 개인(2차) · 공용
     │  ⑤ 우리집 사진관이 보여 줌
     ▼
   가족
```

정리가 끝나면 Photo Desk 가 1차 구역을 비웁니다(`nas_purge`).
**비우는 조건이 셋입니다** — ① 원장(`nas_pulls`)에 내려받은 기록이 있고,
② 맥 작업대에서 사라졌고(정리되어 나갔다는 뜻), ③ 정리 기록이나 휴지통에 흔적이 있을 때.
셋을 다 만족해야 지웁니다. 하나라도 빠지면 남겨 둡니다.

---

## 3. 공유 자원 — 여기가 종속성의 전부입니다

세 앱은 **코드를 공유하지 않습니다.** 서로 부르지도 않습니다.
오직 **NAS 위의 같은 폴더**를 통해서만 이어집니다. 그래서 종속성은 «경로 합의» 하나뿐이고,
그 합의가 어긋나면 아무도 오류를 내지 않은 채 조용히 동작하지 않습니다.

### 3.1 반드시 같은 곳을 가리켜야 하는 설정

| 무엇 | Photo Backup | Photo Desk | 우리집 사진관 |
|---|---|---|---|
| **1차 구역** | 설정 › 업로드 폴더 (`uploadRoot`) | 설정 › NAS (`nas.config.zone1`) | 구역 등록 (`zones.root_path`) |
| 기본값 | `/homes/<계정>/Personal/<기기>` | `/volume1/homes/luckyguy/Personal` | (없음 — 손으로 등록) |
| 개인(2차) | — | `nas.config.photos` | Synology Photos 인덱스 |
| 공용 | — | `nas.config.shared` | Synology Photos 인덱스 |

> **✅ 기본값을 맞췄습니다 (2026-09-01).** Photo Backup 의 기본 업로드 폴더가
> `/homes/<계정>/Personal/<기기>` 로 바뀌어 Photo Desk 의 기본 1차 구역과 같은 곳을
> 가리킵니다. 옛 기본값(`/homes/<계정>/Photos/MobileBackup/<기기>`)이 저장된 폰은
> 다음 로그인 때 한 번 자동으로 옮겨 갑니다 — 손으로 정한 값은 건드리지 않습니다.
> 그 옛 자리는 Photo Desk 가 **2차 구역**(정리가 끝난 내사진)으로 보는 곳이라,
> 거기로 올리면 아무도 가져가지 않았습니다.
>
> **한쪽만 손으로 바꾸면 다시 어긋납니다.** 어긋나도 아무도 오류를 내지 않습니다 —
> Photo Backup 은 계속 올리고 Photo Desk 는 «새 사진 없음» 이라고만 말합니다.
>
> 현재 쓰는 값: `/volume1/homes/luckyguy/Personal`
> (그 아래 기기별 폴더 — 실측 `zFold8`, `현준의 Z Flip8`)

### 3.2 폴더 이름 규칙

Photo Backup 이 만드는 폴더 모양을 Photo Desk 가 읽습니다:

- 폰 폴더 이름이 이미 날짜로 시작하면(`2021-05-13 여행`) **그대로 씁니다.**
- 아니면(`DCIM/Camera`) 뒤에 `yyyy-MM` 을 한 겹 붙입니다 → `DCIM/Camera/2026-07`.

Photo Desk 가 정리해 내보내는 모양은 구역마다 다릅니다:

- **내사진** — 평평하게 (`2018-07-19 하와이 가족여행/`)
- **공용** — 연도 아래 이벤트 (`2018/2018-07-19 하와이 가족여행/`)

### 3.3 접속 방법 (Photo Desk 만 해당)

- **rsync 는 22번 포트**로 붙습니다. DSM 의 rsync 서비스는 제 sshd 로만 받고,
  일반 sshd(이 맥은 72번)로 들어온 rsync 는 setuid 래퍼가 «Permission denied» 로 거절합니다. (실측)
- **셸 명령은 72번**으로 붙습니다(22번 sshd 는 셸을 거부).
- GUI 앱은 PATH 에 Homebrew 가 없어 `/opt/homebrew/bin/rsync` 를 먼저 찾습니다. openrsync 면 거절합니다.

---

## 4. 무엇을 바꿀 때 무엇이 깨지나

| 이걸 바꾸면 | 이게 영향받습니다 | 어떻게 알아채나 |
|---|---|---|
| Photo Backup 의 업로드 폴더 | Photo Desk 가 새 사진을 못 찾음 | **아무 오류 없음.** 「새 사진 0장」이 계속 뜸 |
| Photo Desk 의 `zone1` | 위와 같음 | 위와 같음 |
| Photo Backup 의 폴더 이름 규칙 | Photo Desk 의 정리 결과 모양 | 정리 뒤 폴더가 이상하게 갈림 |
| NAS 계정·공유 폴더 구조 | 셋 다 | 각자 접속 실패 |
| 개인(2차)·공용 경로 | Drive 동기화 짝이 깨짐 | 맥과 NAS 가 서로 다른 것을 가짐 |
| Photo Desk 의 라이브러리 «영역»(작업대/내사진/공용) | 정리 대상과 내보낼 곳 | 엉뚱한 구역으로 나감 |

**셋 중 하나를 손볼 때는 이 표를 먼저 보세요.**
특히 첫 두 줄은 오류를 내지 않으므로, 바꾼 뒤 실제로 사진이 흘러가는지 확인해야 합니다.

---

## 5. 겹쳐 있는 기능과 그 처리

세 앱이 각자 자라면서 같은 일을 하는 코드가 여러 벌 생겼습니다.
**주 경로를 정하되, 아직 실제 화면과 커맨드가 대체하지 못한 기능을 이관 완료로
쓰지 않습니다.**

| 기능 | 주 경로 | 나머지는 | 상태 |
|---|---|---|---|
| 잡동사니 거르기 (스크린샷·메신저) | **Photo Desk** | 사진관은 코드로만 남김 | ✅ 감춤 |
| 완전 중복 | **Photo Desk** (크기→xxHash→SHA-256→image_hash) | 사진관은 코드로만 남김 | ✅ 감춤 |
| 근접 중복 | **Photo Desk** | 사진관의 phash 를 옮겨 왔다 | ✅ 2026-09-01 |
| 흐림·연사 고르기 | **Photo Desk** | 다른 곳에 없음 | ✅ |
| 선택 사진 임의 이동·복사 · 일반 폴더 CRUD | **Photo Desk**(목표) | 사진관 폴더 화면이 아직 담당 | ⛔ Desk v2 동등 기능 필요 |
| 촬영일 감사·파일 교정 | **Photo Desk**(목표) | 사진관에 파일/인덱스 교정 구현 | ⛔ Desk는 현재 판독만 함 |
| 이벤트 자동 발견 · 내사진→공용 복사 | **Photo Desk**(목표) | 사진관 레거시 코드에만 있음 | ⚠ 이관 설계 완료·구현 대기 |
| 인물 · 장소 판정 | **Photo Desk** (자체 AI) | 사진관은 Synology 결과를 감상용으로 사용 | ✅ |
| Synology 앨범 · 인물 이름/병합 | **우리집 사진관** | Desk의 로컬 메타데이터와 별개 | ✅ 사진관 유지 |
| NAS 1차 구역 수집·검증·비우기 | **Photo Desk** | 사진관 구역 관리는 전환 뒤 제거 | ✅ Desk 경로 있음 |
| 폰 → NAS 백업 | **Photo Backup** | 다른 곳에서 할 수 없음 | ✅ |
| 타임라인 감상 | **셋 다** | 보는 것은 아무것도 바꾸지 않으므로 겹쳐도 됨 | ✅ 겹쳐 둠 |

**현재 사진관의 「정리」 탭은 보입니다** (2026-09-02 코드 기준).
`SHOW_MANAGE=true`, `SHOW_MANAGE_TOOLS=false`라서 중복·잡동사니 도구만 숨고,
폴더 이동·복사·삭제·이름변경과 촬영일 교정은 남아 있습니다. Photo Desk v2가 위 표의
차단 기능을 아직 대체하지 못하기 때문입니다.

> **UI를 감추는 것은 쓰기 차단이 아닙니다.** 사진관 백엔드의 물리 파일 변이 API는
> 그대로 호출할 수 있고, 촬영일 교정을 위해 개인 사진 마운트도 `rw`입니다. Desk의
> 동등 기능을 검증한 뒤 Gallery 서버를 `drain`(신규 쓰기 금지·기존 복구만 허용)으로
> 전환하고, 복구 유예가 끝난 다음 마운트를 `ro`로 바꿔야 합니다. 상세 기능표·작업
> 순서·완료 조건은 [Gallery→Desk 전환 기준](https://github.com/HyunjoonKwak/photo_desk/blob/main/docs/GALLERY_TRANSITION.md)에 있습니다.

**Photo Backup 의 열람 화면은 그대로 둡니다.** 「올린 것이 잘 갔나」 확인하는 용도라
백업의 일부로 봅니다. 감상의 주 경로는 여전히 사진관입니다.

> **✅ 근접 중복은 Photo Desk 가 맡습니다 (2026-09-01 이관 완료).**
> 사진관의 지각 해시(phash)를 Photo Desk 로 옮겨 「줄인 사본」 갈래를 만들었습니다.
> **해시만 옮겨서는 못 썼습니다** — 64비트 해시는 «닮은 사진»과 «같은 사진»을 다 잡아
> 첫 결과의 가장 큰 무리가 연달아 찍은 서로 다른 사진 106장이었습니다. 그래서 이은
> 짝마다 16×16 회색조의 평균 절대 차(MAD ≤ 3.5)로 화소를 직접 견주고, 씨앗 기준으로
> 묶어 사슬을 막고, 한 폴더 안 같은 해상도는 연사로 걸러 냅니다.
> 실측 9,211무리 19,197장 · 26.3GB · 14.3만 장에 31초.
>
> Photo Desk 의 CLIP 코사인(≥0.95)은 여전히 「비슷한 장면」 — **다른 것**을 찾습니다.
> 같은 사진의 사본은 「줄인 사본」이, 닮은 다른 컷은 「비슷한 장면」이 맡습니다.

---

## 6. 각 앱에 기능을 더할 때의 규칙

새 기능이 어디에 들어가야 할지는 이 셋으로 판단합니다:

1. **그곳에서만 할 수 있는 일인가?**
   폰 사진 읽기는 안드로이드만(`MediaStore`), 맥 로컬 파일 조작은 Photo Desk 만,
   가족 계정별 권한은 사진관만 할 수 있습니다.
2. **무거운 판단인가?** — 그러면 Photo Desk 입니다. 전량 사본이 있는 곳이 빠릅니다.
3. **가족이 쓰는가?** — 그러면 사진관입니다. 나만 쓰면 Photo Desk 입니다.

지켜야 할 선:

- **사진관에 정리 기능을 더하지 마세요.** 감상 전용입니다.
- **Photo Backup 에 판정 기능을 더하지 마세요.** 올리는 일만 합니다.
- **Photo Desk 가 NAS 파일을 직접 옮기지 않습니다.** 내려받아 맥에서 정리하고,
  NAS 반영은 Drive 동기화에 맡깁니다. (1차 구역 비우기만 예외 — `nas_purge`)

---

## 7. 이름 체계

앱 이름 · 폴더 이름 · GitHub 저장소 이름을 **모두 같게** 유지합니다 (2026-09-01 정리).

| 화면에 뜨는 이름 | 폴더 · 저장소 | 식별자 |
|---|---|---|
| Photo Desk | `photo_desk` | `com.acut.media` |
| 우리집 사진관 | `photo_gallery` | — |
| Photo Backup | `photo_backup` | `com.nasphoto.app` |

- 화면 이름은 사진관만 한글입니다 — 가족이 보기 때문입니다. 나머지 둘은 내 도구라 영문입니다.
- **식별자는 절대 바꾸지 않습니다.** 옛 이름(`acut`·`nasphoto`)이 남아 있지만,
  바꾸면 앱 데이터 경로가 달라져 DB·설정·백업 이력이 통째로 «없는 것»이 됩니다.

> **⚠ 사진관의 배포 이름은 옛 이름 그대로입니다.** 저장소는 `photo_gallery` 지만
> NAS 배포 경로는 `/volume1/code_work/nas_photo`, 이미지·컨테이너는 `nas-photo` 입니다.
> 맞추면 GHCR 패키지 이력이 끊기고 `./data`(SQLite — 세션·작업로그·사진 해시)가
> 새 경로로 갈라집니다. 위의 «식별자는 바꾸지 않는다» 와 같은 이유입니다.
> 두 이름은 repo 루트의 `ship.conf` 가 잇습니다 — **지우면 배포가 빈 디렉터리로 갑니다**
> (`ship` 의 기본 규칙이 `/volume1/code_work/<폴더명>` 이라 `.env` 없이 새로 만듭니다).
