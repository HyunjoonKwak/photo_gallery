# NAS Photo Organizer — 프로젝트 지침

## 필수 참조 문서
- `NAS_사진정리앱_개발명세서.md` — 전체 명세 (단일 진실 소스)
- `docs/IMPROVEMENTS.md` — 전체 리뷰(2026-07-02)에서 도출된 개선 필요사항 백로그

## 작업 규칙 (MUST)
- **어떤 기능/수정 작업이든 착수 전에 `docs/IMPROVEMENTS.md`의 관련 절을 확인하고, 해당 결정·수정사항을 빠짐없이 반영한다.**
  - 백엔드 작업 → A절(선행 수정) + E절(DSM 연동 참고)
  - 프론트 화면 구현 → B절(UX 결정) + C절(스택·구현 함정)
  - 중복 제거(2단계) → D절
- 항목을 반영/완료하면 `docs/IMPROVEMENTS.md`의 체크박스를 갱신해 문서와 코드를 동기화한다.
- 1단계 기능 개발 전에 A절 HIGH/MED 항목(A-1~A-4)을 먼저 해소한다.
- 명세서와 IMPROVEMENTS.md가 충돌하면 사용자에게 확인 후 명세서를 갱신한다(둘 다 최신 유지).

## 아키텍처 불변 원칙
- 파일 조작은 직접 볼륨 마운트가 아니라 **DSM Web API로만** 수행(권한은 DSM이 enforce).
- DSM `sid`는 브라우저에 절대 노출하지 않음(서버측 세션 매핑 유지).
- API 경로/버전은 하드코딩하지 않고 `SYNO.API.Info` 런타임 프로브 사용.
- 파괴적 작업은 항상 가역(휴지통 + 작업로그 + Undo).
